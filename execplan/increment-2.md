# Complete Increment 2: intelligence, policy, providers, and operability

This ExecPlan is a living document. Keep `Progress`, `Surprises & Discoveries`, `Decision Log`, `Outcomes & Retrospective`, and the revision notes current whenever implementation changes direction or stops. The authoritative contracts are `project.md` for the completed Increment 1 appliance and `LOCAL_CODE_MAINTAINER_INCREMENT_2_GOAL.md` for this increment. The machine-readable requirement map is `config/increment-2-coverage.json`.

## Purpose / Big Picture

Increment 2 turns the working local maintenance appliance into an explainable heterogeneous code-maintenance platform without weakening Increment 1. An operator can onboard PHP, .NET/Windows, R, and mixed repositories through a browser; inspect evidence-backed Repo Doctor proposals; assign pinned capability packs; clarify and approve a task contract; see risk, baseline, code impact, context, tests, documentation, model, scheduling, policy, and egress decisions; run the workflow with deterministic local fakes; and trace the resulting code, verification, findings, documentation, memory, commit, forge result, and SBOM through one evidence graph.

The controller remains the sole workflow-state authority. New parsers, forges, providers, workers, policy engines, schedulers, indexes, and caches stay behind narrow domain interfaces. The browser and CLI call the same controller services. Workers never receive a Docker socket or forge credentials, remote inference is disabled by default, and no endpoint accepts arbitrary commands, images, mounts, paths, networks, model arguments, provider URLs outside approved profiles, or unrestricted environment variables.

The locally testable outcome uses a protocol-accurate fake provider gateway, fake GitHub and GitLab services, local bare Git, a narrow simulated Windows worker, small fixture repositories, and fake local inference. Real forge credentials, a licensed Windows worker, proprietary hardware, remote-provider accounts, signing infrastructure, and large model weights remain finite operator-only validations with safe fakes and exact procedures.

## Progress

- [x] (2026-07-20 20:46Z) Read the complete Increment 2 contract, repository guidance, base product contract, completed Increment 1 plan, current migrations, configuration/storage/API/CLI/UI seams, and current package inventory.
- [x] (2026-07-20 20:46Z) Established the pre-change baseline: containerized Go tests passed, frontend tests passed with two tests and no critical accessibility violations, Compose validation passed, and generated OpenAPI client drift passed after the required per-container `npm ci`.
- [x] (2026-07-20 20:46Z) Created this self-contained living plan and the machine-readable requirement-to-implementation/test matrix.
- [x] (2026-07-20 21:05Z) Added the trusted pure configuration registry for all 13 Increment 1 system-document fields, explicit bootstrap-only descriptors, strict unknown-key/scope/value rejection, deterministic seven-layer precedence with provenance, write-only redaction, and deterministic complete snapshot hashing; focused and full Go tests pass.
- [x] (2026-07-20 21:09Z) Added forward-only migration 16 and transactional scoped-value persistence: active Increment 1 import, optimistic scope heads, redacted append-only revisions, atomic reset-to-inherited, immutable job snapshots, same-scope rollback binding, audit events, restart-safe reads, and regression tests; full Go tests pass.
- [x] (2026-07-20 21:27Z) Added the shared registry application service and draft/check lifecycle: trusted normalization, exact-version create/update/review, append-only validation/dry-run results, review-to-apply content binding, reauthentication signal for secret descriptors, effective configuration, whole-scope historical rollback, complete job snapshots, and atomic Increment 1 system-document compatibility projection; full Go tests pass.
- [x] (2026-07-20 21:38Z) Wired the registry into controller startup and added typed REST/OpenAPI and CLI surfaces for descriptors/search, scoped values, effective resolution, draft create/update/read/list/check/validate/dry-run/review/apply/discard, revision history, and rollback. ETags are mandatory for writes, API lifecycle/CLI header tests pass, Redocly reports a valid contract without warnings, and generated TypeScript bindings match.
- [x] (2026-07-20 22:00Z) Added deterministic redacted declarative export, hash/schema/registry-bound strict and forward-compatible import previews, reviewed import drafts, immutable preservation of unknown forward keys that are never applied, prerequisite discovery, REST/OpenAPI/CLI parity, and forward-only migration 17. Restored migration 16 exactly and proved retained version-16 drafts upgrade with `operation=apply`. A patched `js-yaml` 4.3.0 override clears a newly published transitive advisory; Go tests/vet, npm audit/test/build, Redocly, generated API drift, and Compose validation pass.
- [x] (2026-07-20 22:25Z) Added the dedicated accessible Configuration workbench with safe/advanced registry search, typed checkbox/number/multiselect/list controls, six mutable scope choices, provenance, effective/current-scope state, reset-to-inherited, apply badges, diff preview, draft edit/check/review/apply/discard, redacted import/export, prerequisite view, and reauthenticated additive rollback. Added the 13-setting machine-readable coverage inventory and CI equality/completeness gate. Configured controller stores now snapshot complete effective system/project configuration in the same transaction as every direct, Hermes, or scheduled accepted job; Job inspection exposes the immutable redacted hashes/document. Focused Go and three frontend tests pass with no WCAG A/AA violations; generated client and production build pass.
- [x] (2026-07-20 22:42Z) Closed Milestone 1 with deterministic conditional dependency/incompatibility validation across draft, import, check, review, and apply; compiled fail-closed prerequisite/dry-run handlers; a passing durable-storage/local-inbox readiness probe; browser-side relation previews, explicit inherited/safe-default resets, true before/after impact display, and corrected system-layer resolution for non-system scopes. Full Go tests and vet, four frontend tests, production build, generated-client drift, Redocly, and Compose validation pass. The skill-directed real-browser overflow/focus pass remains a documented Milestone 7 check because host `npx` is unavailable; no browser result is claimed.
- [ ] (2026-07-20 23:10Z) Milestone 2 in progress: added migration 18; project/repository/blob/parser-keyed incremental analysis with exact Go syntax and bounded lexical adapters; partial-failure evidence; a bounded credential-isolated Git snapshot route; status/query/refresh/rebuild REST/OpenAPI/CLI/browser surfaces; production workflow-stage Context Compiler manifests; five-way differential primitives; explainable test impact with mandatory full-suite policy; and project/trust-domain cache metadata with conflict, retention, reauthentication, purge, and audit controls. Focused Go/API/Git/SQLite tests and five frontend tests/build/client drift pass. Tree-sitter/SCIP/read-only-LSP enrichment, workflow-stage production of baseline/impact evidence, real cache object verification, cancellation/pause/retention controls, and final-gate integration remain open.
- [ ] (2026-07-20 23:45Z) Milestone 2 workflow integration checkpoint: migration 19 makes baseline, purpose-bound differential, and per-candidate impact identities restart-idempotent. Workflow sync now indexes an exact Git snapshot; reproduction captures an exact configuration/toolchain/pack-bound pre-modification baseline; targeted/full/final checks persist comparable differentials; every committed candidate records changed-path/indexed-symbol impact; and final verification refuses to advance without mandatory full-suite evidence. Parsed-blob rows are registered as independently hashed cache objects with project quota, retention, hit/miss reason, expiry, automatic pruning, cancellation evidence, and audited destructive cleanup. Six new typed settings expose pause, retention, quota, context budgets, and clean-final-cache policy through existing browser/API/CLI parity and immutable job snapshots. Focused Go and frontend gates pass. Rich Tree-sitter/SCIP/read-only-LSP adapters, broader Context Compiler sources/manifest comparison, baseline correction/update policy, test-impact overrides/history, and cache verify/warm/simulation actions remain open.
- [ ] (2026-07-21 00:01Z) Milestone 2 operator-action checkpoint: deterministic project-scoped context-manifest comparison and read-only cache integrity verification, quota simulation, and trusted exact-snapshot warming now have REST/OpenAPI/generated-client/CLI/browser parity. Parsed-source verification recomputes complete key/input/object identities; unsupported cache kinds report unavailable rather than passing. Service, SQLite, API, CLI, OpenAPI, and accessible UI regressions pass. Rich Tree-sitter/SCIP/read-only-LSP adapters, broader Context Compiler sources, explicit baseline correction/update policy, and test-impact overrides/history keep the milestone open.
- [ ] (2026-07-21 00:16Z) Milestone 2 evidence-review checkpoint: forward-only migration 20 adds immutable, attributed, audited baseline supersessions, differential corrections, and targeted-test overrides. Replacement baselines derive only from stored candidate observations; newly introduced findings cannot be classified away as pre-existing/resolved; targeted overrides cannot change the full-suite requirement. REST/OpenAPI/generated-client/CLI/browser histories and actions pass focused service/storage/API/CLI/frontend tests and build. Rich adapters and broader Context Compiler sources keep the milestone open.
- [x] (2026-07-21 00:46Z) Closed Milestone 2: added a pinned real Tree-sitter service for Python, JavaScript/TypeScript/TSX, PHP, Rust, C/C++, C#, and R while retaining exact native Go AST extraction and the controller's static non-CGO image. The isolated service is non-root/read-only/no-mount/no-published-port; its one-file hash-bound protocol and SCIP/read-only-LSP enrichment seam cannot execute workspace commands, and controller validation rejects unsafe facts. Parser/tool provenance is visible. Production context now includes immutable config, verifier policy, repository instructions/docs, exact indexed ranges with whole-file fallback, verified canonical memory, unresolved findings, and recent baseline/differential/impact evidence. Normal and explicit CGO-disabled tests, Compose validation, both image builds, and live internal health/analysis probes pass.
- [x] (2026-07-21 01:31Z) Closed Milestone 3: forward-only migration 21 adds transactional capability installations, append-only lifecycle history, exact project assignments, Repo Doctor scans, and optimistic proposal reviews. Canonical manifests are checksummed and authority-neutral; lifecycle changes require reauthentication and preserve assignment versions. Repo Doctor reads only an exact bounded Git-bridge snapshot, rejects browser bytes, persists confidence/path/hash evidence and disabled proposals, computes re-scan drift, and assigns a pack only through explicit exact-version review. Shipped PHP 8.3, R statistical, and SBOM/FMEA/security manifests plus reusable rehearsal primitives and complete REST/OpenAPI/generated-client/CLI/accessible-browser/docs parity. Full Go/vet, focused race, seven frontend tests/build, client drift, zero-warning Redocly, and Compose validation pass.
- [ ] (2026-07-21 02:38Z) Milestone 4 forge-foundation checkpoint: migration 22, `ForgeProvider`, credential-safe Git-bridge probe/sync routes, GitLab token/configuration/draft-MR support, normalized profile/object/run storage, exact endpoint admission, credential-reference reauthentication, optimistic revisions, bounded provider metadata, cursor/idempotency replay, GitHub/GitLab/local project registration, REST/OpenAPI/generated-client, and `maintainctl forge` operations are implemented. Focused Go tests and zero-warning OpenAPI generation pass. Hosted object inventory, webhook normalization, browser operations, simulated Windows worker, fixtures, failure/restart suites, documents, and the complete milestone gate remain open; work paused for the operator-requested product rename and initial GitHub synchronization.
- [ ] (2026-07-22 08:13Z) Milestone 4 hosted-forge and browser checkpoint: GitHub and GitLab now collect bounded normalized issue/change-request/CI/artifact/ref/release inventory through GET-only transient retries, provider pagination cursors, rate-limit pause state, exact project identity binding, explicit unavailable-capability evidence, and retained provider metadata. GitHub App permissions add read-only Actions access; GitLab job artifact metadata is normalized without downloading archives. The responsive Git forges workbench replaces the GitHub-only page with versioned endpoint/allow-list/polling/convention/label/artifact/release/submodule controls, write-only credential-reference input, probes, sync history, and normalized-object browsing. Focused Go tests and the production frontend build pass; the complete frontend suite, docs, GitLab webhook normalization, restart/API failure cases, Windows simulator, fixtures, and the full milestone gate remain open.
- [ ] (2026-07-22 08:40Z) Milestone 4 Windows-worker checkpoint, paused at operator request: migration 23, a pure closed Windows-worker domain contract, optimistic/audited profiles, durable idempotent structured results, authenticated bounded worker protocol, deterministic simulator, and the `windows-dotnet-labautomation` capability pack cover ten fixed operation classes without accepting commands, scripts, images, mounts, paths, networks, environment, or arguments. The controller seeds only the safe simulator, and REST/OpenAPI/generated-client, `maintainctl windows-worker`, and an accessible Runners and Windows workbench expose inventory, gates, probes, fixture runs, and evidence. Full Go tests/vet, six frontend files with nine tests, production build, zero-warning OpenAPI/client drift, and Compose validation pass. Real remote-adapter routing, heterogeneous repository fixtures, GitLab webhook normalization, broader failure/restart suites, operator documentation, coverage-map closure, and the full Milestone 4 gate remain open.
- [ ] (2026-07-22 09:07Z) Milestone 4 remote-worker/forge checkpoint, paused and synchronized at operator request: controller composition now routes simulator profiles locally and remote profiles through the authenticated bounded protocol using a fixed-directory opaque credential resolver that rejects paths, symlinks, non-regular files, short/oversized tokens, and remote-to-simulator fallback. PHP 8.3, pinned .NET/PowerShell/Inno/HAMILTON, R, and deliberately malformed mixed repository fixtures exercise evidence-backed Repo Doctor detection. GitLab merge-request webhooks now validate the provider token in the credential-isolated Git bridge, normalize exact-byte events with GitHub-compatible replay semantics, and persist through forward-only migration 24; operator forge and Windows-worker documentation plus the machine-readable coverage evidence are current. Full Go tests/vet, focused race tests, six frontend files with nine tests/build, zero-warning OpenAPI/client drift, and Compose validation pass. Milestone 4 remains open for the broader restart/migration/failure matrix, specialized capability-editor audit, and the complete acceptance gate.
- [x] (2026-07-22 10:02Z) Closed Milestone 4 after replacing the R/security raw JSON editor with controller-manifest-driven typed fields, closed-schema normalization, default expansion, no-write previews, optimistic audited assignment updates, API/OpenAPI/CLI parity, and specialized operator documents. Restart evidence now covers forge profiles/cursors/objects/idempotency, Windows runs, and capability configurations; retained migration 1→24 and migration-24 compatibility plus malformed provider/webhook/worker, stale-revision, credential, cross-project, and unknown-field cases complete the failure matrix. Full Go tests/vet, focused races, ten frontend accessibility tests/build, zero-warning OpenAPI/client drift, Compose validation, control-plane image builds, authenticated doctor, health, and isolated desktop/mobile Chromium acceptance pass. The retained pre-rename stack remained healthy throughout.
- [ ] (2026-07-22 10:11Z) Paused at the operator-requested post-Milestone-4 sync point. Commit `3e19b15` is on `origin/dev` and GitHub Actions run `29910740641` passed every CI job. Milestone 5 is the next untouched implementation boundary; no task-contract, risk, agent-contract, Test Designer, golden, documentation-policy, or OPA state has been introduced by this checkpoint.
- [ ] (2026-07-23 21:40Z) Milestone 5 task-contract/risk foundation checkpoint: forward-only migration 25 adds durable task contracts, append-only contract events, deterministic risk assessments, and expiring risk waivers. The workflow now pauses at `awaiting_task_approval`; approval stores the exact accepted criteria hash and advances implementation. REST/OpenAPI/generated-client, `maintainctl contract`, `maintainctl risk`, and the Jobs page expose contract/risk evidence and approval. Risk lowering uses server-side recent reauthentication, not a request-body claim. Full Go tests plus frontend API drift, TypeScript/tests, and Redocly lint pass. Agent-contract registry, independent Test Designer, golden/rehearsal gates, Documentation Agent/QC, and OPA lifecycle remain open.
- [ ] (2026-07-23 22:25Z) Milestone 5 agent-contract checkpoint: migration 26 adds an append-only schema-validation ledger, `internal/agents` now registers versioned JSON Schema contracts for task packets, implementation results, QC reports, Test Designer proposals, documentation manifests, risk assessments, and completion summaries, and model calls request strict JSON Schema response format before existing controller validation. Workflow phases record schema/payload hashes, REST/OpenAPI/generated-client/CLI expose the catalogue and per-job validation evidence, and the Jobs page shows structured-output contract history. Focused Go/API/SQLite/workflow tests pass; full retry orchestration, Test Designer, golden/rehearsal, documentation QC, and OPA remain open.
- [ ] (2026-07-23 23:10Z) Milestone 5 independent Test Designer checkpoint, paused and synchronized at operator request: migration 27 adds append-only Test Designer reports; medium/high-risk jobs route from full verification through `loading_test_designer_model` and `test_design_review` before QC; low-risk seeded phases store explicit skipped evidence. The Test Designer task packet is exact result/risk/contract bound, model output is strict `test_proposal` JSON, reports are visible through REST/OpenAPI/generated-client, `maintainctl test-designer`, and the Jobs page, and recent reports become context for later agents. Full Go tests, full race tests, Go vet, frontend API drift/tests/typecheck/build, Redocly lint, and Compose validation pass. Milestone 5 remains open for disposition enforcement/retry orchestration, golden/rehearsal gates, Documentation Agent/QC, and OPA policy lifecycle.
- [ ] (2026-07-23 23:35Z) Milestone 5 golden/rehearsal foundation checkpoint: migration 28 adds append-only golden rehearsal reports and update approval records; the workflow inserts `golden_rehearsal_review` before QC after Test Designer or whenever low-risk work has registered pack rehearsals; reports select only trusted capability rehearsal definitions and matching Test Designer suggestions; changed goldens fail closed with approval-required evidence instead of updating themselves. REST/OpenAPI/generated-client, `maintainctl golden`, and the Jobs page expose comparisons, tolerance/mask metadata, provenance, and approvals. Focused Go tests, full Go tests, frontend API drift/tests/typecheck, and Redocly lint pass. Milestone 5 remains open for rendered visual diffs, editable tolerance/mask management, disposition enforcement, Documentation Agent/QC, and OPA policy lifecycle.
- [ ] (2026-07-23 23:39Z) Milestone 5 Documentation Agent foundation checkpoint: migration 29 adds append-only documentation manifests; `loading_documentation_model` and `documentation_review` now run after full/golden evidence and before QC; documentation manifests are exact job/project/contract/risk/result bound; documentation commits update `result_sha`, record fresh test-impact evidence, and run fresh full verification before QC. The `documentation` worker kind is digest-pinned and inference-only, mock profiles include deterministic source-controlled docs, and REST/OpenAPI/generated-client, `maintainctl documentation`, and the Jobs page expose requirements, changes, checks, edits, and unsupported claims. Focused Go and frontend/OpenAPI checks pass. Milestone 5 remains open for the full declarative documentation policy/toolchain/QC workbench, Test Designer disposition enforcement, golden visual diff/tolerance management, and OPA policy lifecycle.
- [ ] (2026-07-24 00:18Z) Milestone 5 OPA policy lifecycle foundation checkpoint: migration 30 adds append-only policy bundles, activations, simulations, and decisions. The workflow inserts `policy_review` after Documentation Agent evidence and before QC model loading; the active structured policy bundle records redacted inputs, required stages, bundle version, allow/deny explanation, and fail-closed state. REST/OpenAPI/generated-client, `maintainctl policy`, and the Jobs page expose bundle, activation, simulation, and job decision evidence. Focused Go tests, full Go tests, Go vet, frontend API drift/tests/build, OpenAPI lint, and Compose validation pass. Milestone 5 remains open for full embedded OPA/Rego authoring, formatting/tests/coverage, structured editor UX, rollback workbench, and broader protected-action enforcement.
- [ ] (2026-07-24 00:48Z) Paused at an operator-requested Milestone 5 sync point after adding the embedded OPA/Rego policy-test checkpoint: migration 31 adds append-only retained test runs; the controller formats advanced Rego, pins source and formatted-source hashes, evaluates through the embedded OPA library, requires a passing retained test run for the exact formatted source before advanced activation, and fails closed on OPA errors. REST/OpenAPI/generated-client, `maintainctl policy create-advanced|test|test-runs`, and the Policy page expose source, structured interpretation, tests/coverage, simulations, activation history, and expert-only Rego authoring. Validation passed for focused Go packages, `go test ./...`, `go test -race ./...`, `go vet ./...`, frontend API drift/tests/build, OpenAPI lint, and Compose validation. Milestone 5 remains open for structured template editing, richer staged-rollout/rollback workflows, Test Designer disposition enforcement, golden visual diff/tolerance management, full documentation policy/QC, and cross-domain protected-action enforcement.
- [ ] (2026-07-24 01:09Z) Milestone 5 Test Designer disposition enforcement checkpoint: migration 32 adds an append-only disposition ledger; required pending proposals now route jobs into `awaiting_test_design_disposition` before golden, documentation, policy, or QC gates. Effective report views overlay the latest reviewer/admin accepted, rejected, or not-applicable disposition without mutating the original Test Designer report. REST/OpenAPI/generated-client, `maintainctl test-designer dispose`, and the Jobs page expose the disposition workflow. Focused Go and frontend checks pass. Milestone 5 remains open for golden visual diff/tolerance management, full documentation policy/QC, structured policy editing, richer staged rollout/rollback, and cross-domain protected-action enforcement.
- [ ] (2026-07-24 01:28Z) Milestone 5 agent-contract retry checkpoint: model-backed implementation, QC, Test Designer, and Documentation Agent workers now apply the registered structured-output retry policy. Malformed JSON, wrong task bindings, unsafe or extra fields, unsupported claims, and schema/bounds violations are rejected before trusted use; the next attempt receives bounded controller validation feedback, and exhaustion fails the phase after three attempts. Focused `internal/agents`, `internal/workflow`, and `cmd/maintainer-worker` tests pass. Milestone 5 remains open for golden visual diff/tolerance management, full documentation policy/QC, structured policy editing, richer staged rollout/rollback, and cross-domain protected-action enforcement.
- [ ] (2026-07-24 01:37Z) Milestone 5 golden approval wait checkpoint: approval-required golden/rehearsal reports now route jobs to non-resumable `awaiting_golden_approval` instead of failing the phase. Golden approvals and rejections remain append-only; the latest decision per comparison determines whether required updates are still pending, and once all required comparisons are approved the API advances the job to `loading_documentation_model`. REST/OpenAPI/generated-client and the Jobs page expose the wait-state resolution. Focused Go and frontend checks pass. Milestone 5 remains open for rendered visual diffs and editable tolerance/mask management, full documentation policy/QC, structured policy editing, richer staged rollout/rollback, and cross-domain protected-action enforcement.
- [ ] (2026-07-24 01:53Z) Milestone 5 typed golden tolerance/mask profile checkpoint: migration 33 adds append-only golden comparison profiles; reviewer/admin operators can record typed absolute/relative tolerance, dynamic-region masks, and explicit mask selectors without mutating immutable reports or approvals. REST/OpenAPI/generated-client, `maintainctl golden configure-profile`, job-detail evidence, and the Jobs page expose profile history and a structured diff preview with summary/provenance/hash/policy evidence. Focused Go packages, frontend API drift/tests/build, and generated-client drift pass. Milestone 5 remains open for richer rendered visual diff artifacts, full documentation policy/QC, structured policy editing, richer staged rollout/rollback, and cross-domain protected-action enforcement.
- [ ] (2026-07-24 02:09Z) Milestone 5 documentation-policy finding lifecycle checkpoint: blocked Documentation Agent manifests now convert pending/blocked requirements, failed checks, unsupported claims, and generic blocked policy evidence into stable retained findings. The existing finding state machine handles dispute/accept/waive/escalate, and repair-cycle review permits only documentation policy categories rather than arbitrary new blockers. The Jobs page shows documentation finding counts and expert evidence alongside manifests. Focused workflow/storage tests and frontend API drift/tests/build pass. Milestone 5 remains open for the declarative documentation policy builder, renderer/tool profiles, screenshot/golden management, rich render previews, independent documentation QC actions, structured policy editing, richer staged rollout/rollback, and cross-domain protected-action enforcement.
- [ ] (2026-07-24 02:30Z) Milestone 5 documentation policy simulation checkpoint: `internal/docagent` now exposes a controller-owned versioned documentation policy profile with typed rules, match criteria, tool profile metadata, source-of-truth mappings, reviewer roles, render targets, checks, and publication gates. REST/OpenAPI/generated-client, `maintainctl documentation policy|simulate`, and the Policy page expose bounded change-impact simulation from paths/classes/risk/languages/packs/labels without executing repository tools. Focused Go and frontend API drift/tests/build pass. Milestone 5 remains open for persistent editable documentation policy/tool profiles, render previews, independent documentation QC actions, structured policy editing, richer staged rollout/rollback, and cross-domain protected-action enforcement.
- [ ] (2026-07-24 03:25Z) Paused at an operator-requested sync point after starting Milestone 6 with the advisory resource scheduler foundation: migration 34 adds append-only retained scheduler decisions; `internal/scheduler` simulates quality/latency and throughput/batching decisions against bounded topology, profiles, active work, maintenance windows, fairness, memory, and CPU pressure; REST/OpenAPI/generated-client, `maintainctl scheduler`, and the Automation page expose status, queue simulation, and decision history. Focused Go and frontend checks pass. Milestone 6 remains open for binding live queue leases to scheduler decisions, provider-neutral inference, runtime lab, evidence graph, evaluation isolation, OpenTelemetry, and support bundles.
- [ ] (2026-07-24 03:55Z) Milestone 6 live scheduler lease checkpoint: durable job lease acquisition now builds a scheduler request from active unexpired leases and resumable unlocked candidates in the same SQLite transaction, maps workflow states to bounded controller/dependency/verification/model profiles, records selected/deferred decisions append-only, defers same-project concurrent candidates for fairness, and refuses candidates whose declared profile would exceed CPU or memory capacity after active leases and controller reservation. Focused scheduler/storage/queue tests, full Go tests, and Go vet pass. Milestone 6 remains open for richer NUMA/disk/I/O/thermal/provider-health inputs, configurable limits and batching windows, safe benchmark recommendations, provider-neutral inference, runtime lab, evidence graph, evaluation isolation, OpenTelemetry, and support bundles.
- [ ] (2026-07-24 04:20Z) Milestone 6 provider-gateway foundation checkpoint: migration 35 adds versioned provider, endpoint, model, route, and append-only egress-manifest records; `internal/providers` now seeds local llama.cpp plus disabled fake remote provider templates, validates endpoint trust/network/TLS/SSRF constraints, routes only enabled eligible models, rejects forbidden data classes, enforces structured-output/cost/token constraints, and records allowed or denied egress manifests with hashes. REST/OpenAPI/generated-client, `maintainctl model-provider`, the Models page, docs, and coverage evidence are current. Focused provider/storage/API/CLI tests and a targeted accessible frontend build pass. Milestone 6 remains open for real protocol adapters, write-only credential rotation, capability probing/drift, gateway integration into model-backed workers, runtime optimization lab, evidence graph, evaluation isolation, OpenTelemetry, and support bundles.
- [ ] (2026-07-24 04:25Z) Paused at the operator-requested sync checkpoint after adding migration 36 append-only capability probes, deterministic fake adapters for every required provider family, retained request/response schema hashes and native API-shape observations, REST/OpenAPI/generated-client/CLI probe operations, Models-page probe controls/history, provider documentation, and coverage-map evidence. Focused provider/storage/API/CLI tests plus frontend API drift and `src/App.test.tsx` pass; broad validation and git synchronization are the current handoff gate.
- [ ] (2026-07-24 04:42Z) Milestone 6 runtime benchmark lab checkpoint: migration 37 adds append-only local runtime benchmark evidence. The existing allow-listed model benchmark action now retains exact manifest/runtime identity, smoke quality fixture status, determinism status, prompt/decode timing, duration, memory, cache eligibility, experimental feature gates, and candidate/rejected recommendation without activating profiles or optimizations. REST/OpenAPI/generated-client, `maintainctl model benchmarks`, Models-page runtime lab history, model docs, and coverage evidence are updated. Focused runtime/storage/API/CLI and frontend API/App tests pass; profile activation/rollback workflows and richer benchmark fixtures remain open.
- [ ] (2026-07-24 05:08Z) Milestone 6 typed evidence graph checkpoint: migration 38 adds append-only evidence nodes and edges. Artifact indexing now transactionally retains job/artifact nodes and a typed `produced` edge alongside the artifact row and audit event. REST/OpenAPI/generated-client, `maintainctl evidence <job-id>`, Jobs-page traceability display, docs, and coverage evidence are updated. Focused evidence/storage/API/CLI and frontend API/App/OpenAPI tests pass; complete request-through-publication/SBOM graph coverage remains open.
- [ ] (2026-07-24 05:35Z) Milestone 6 historical evaluation foundation checkpoint: migration 39 adds append-only evaluation datasets and run reports. The new offline deterministic simulator hides known patches behind derived hashes, uses `eval://memory/...` and `eval://cache/...` namespaces, records profile comparison metrics and reproducible report hashes, and blocks automatic model/policy/route promotion. REST/OpenAPI/generated-client, `maintainctl evaluation`, Evaluation-page UI, docs, and coverage evidence are updated. Focused validation is the current gate before the requested sync.
- [ ] (2026-07-24 06:20Z) Paused at the requested sync checkpoint after adding the first bounded redacted observability and support-bundle foundation. Migration 40 stores append-only local telemetry events and support bundle manifests; `internal/observability` redacts secrets, prompts, request bodies, unrestricted source content, and hidden reasoning before storage; REST/OpenAPI/generated-client, `maintainctl observability`, Observability-page UI, docs, and coverage evidence are current. External OTLP export remains disabled/default-off, and full cross-component OpenTelemetry instrumentation remains open.
- [ ] (2026-07-24 06:45Z) Milestone 7 dashboard information-architecture checkpoint: the operator console now exposes the required 17 named navigation areas, remaps existing operations to the contract labels, folds administration into Setup and health, and adds dedicated Documentation plus Security and SBOM pages backed by controller policy/profile, manifests, capability-pack, finding, rehearsal, and evidence data. `web/src/App.test.tsx` asserts all 17 areas and runs axe checks over the new pages; full frontend tests, production build, generated API drift check, and `go test ./internal/config` pass. Direct per-domain filtered configuration backlinks and full browser/keyboard E2E remain open.
- [ ] (2026-07-24 06:55Z) Milestone 7 dashboard/configuration link checkpoint: every non-configuration dashboard area now exposes a parameterized link to the filtered Configuration workbench, the Configuration workbench consumes `#configuration?search=...&from=...` route parameters, and every visible setting card renders consumer backlinks to the feature/status pages that use it. The registry search now token-matches multi-area filters. Focused App and Configuration tests cover the forward route and reciprocal links; full browser/keyboard E2E remains open.
- [ ] (2026-07-24 07:20Z) Milestone 7 migration/restore evidence checkpoint: `CurrentSchemaVersion` is now the single controller constant for schema v40, `allMigrations()` centralizes the embedded forward migration list, and `config/migration-coverage.json` inventories every migration file with durable records, restart/resume evidence, backup/restore evidence, documentation, and rollback guidance. The SQLite migration test now fails if a migration is added without inventory evidence, and the backup test verifies dry-run, staged restore, pre-restore database preservation, WAL/SHM cleanup, and restored database content. Focused validation is the current gate; the broader migration-from-every-supported-version and full backup/rollback acceptance remain open.
- [ ] (2026-07-24 07:35Z) Milestone 7 external-validation coverage checkpoint: `config/external-validation-coverage.json` now maps the eight operator-only acceptance areas to prerequisites, safe fake evidence, finite operator procedures, and explicit CI non-claims. `internal/config/external_validation_coverage_test.go` fails if an external-only item lacks fake evidence or a documented procedure. Focused validation is pending; the row remains partial until the full final acceptance report reconciles every operator-only boundary.
- [ ] (2026-07-24 07:55Z) Milestone 7 configuration parity coverage checkpoint: `config/configuration-parity.json` now maps routine typed-registry operations and legacy system-document compatibility operations to REST/OpenAPI routes, `maintainctl config` commands, the browser workbench, docs, and tests. `internal/config/configuration_parity_test.go` checks that each required operation exists in the matrix, that every REST path appears in OpenAPI, that CLI command strings remain present, and that UI/test/doc evidence paths exist. Focused validation is pending; full browser/keyboard E2E and final parity acceptance remain open.
- [ ] (2026-07-24 08:05Z) Paused at the operator-requested sync point before starting the cross-surface secret-redaction coverage slice. Commit `f6d25b6` was already on `origin/dev` and GitHub Actions run `30076306125` passed; the next resumption boundary is `I2-DOD-07` write-only secret redaction coverage plus remaining Milestone 7 acceptance.
- [ ] (2026-07-24 08:25Z) Milestone 7 redaction coverage checkpoint: `config/redaction-coverage.json` now maps ten secret-bearing output surfaces across configuration, provider egress, observability events, support bundles, memory, auth/session state, forge credentials, Windows-worker credentials, and policy inputs. `internal/config/redaction_coverage_test.go` checks required surface coverage, referenced implementation/UI/test/doc paths, and proof snippets for existing leak-prevention regressions. Focused config validation passes; broader redaction-adjacent packages and final E2E/security acceptance remain open.
- [ ] (2026-07-24 08:45Z) Milestone 7 DOD-11 intelligence evidence checkpoint: `config/intelligence-coverage.json` now maps seven surfaces for project-separated incremental indexing, deterministic context manifests, workflow baselines, differential verification, test-impact records, cache integrity/quota/retention, and REST/CLI/browser parity. `internal/config/intelligence_coverage_test.go` verifies required surface coverage, referenced evidence paths, and proof snippets; full acceptance remains open.
- [ ] (2026-07-24 09:05Z) Milestone 7 DOD-13 verification-safety checkpoint: `config/verification-safety-coverage.json` now maps non-waivable newly introduced findings, mandatory final full-suite impact evidence, explicit golden-update approval, and REST/CLI/browser parity. `internal/config/verification_safety_coverage_test.go` verifies required surface coverage, evidence paths, and proof snippets; broader final acceptance remains open.
- [ ] (2026-07-24 09:25Z) Milestone 7 DOD-14 documentation-policy checkpoint: `config/documentation-policy-coverage.json` now maps policy impact selection, source-controlled Documentation Agent generation, render/check validation blockers, fresh verification/traceability, and REST/CLI/browser parity. `internal/config/documentation_policy_coverage_test.go` verifies coverage, proof snippets, and explicit remaining documentation QC/tool-profile work.
- [ ] (2026-07-24 09:45Z) Milestone 7 security/adversarial checkpoint: `config/security-adversarial-coverage.json` now maps authorization/reauthentication/stale-edit gates, untrusted-input injection rejection, worker container isolation, provider SSRF/egress/fallback/cost controls, forge webhook authentication, cross-project memory/evaluation isolation, backup restore path safety, no-write previews, and redaction to existing implementation/tests/docs/UI. `internal/config/security_adversarial_coverage_test.go` verifies the required coverage and proof snippets, and `internal/backup/manager_test.go` now has an explicit unsafe pending-restore path regression. Increment 2 remains active pending broader final acceptance.
- [ ] (2026-07-24 10:05Z) Milestone 7 unit/property/fuzz checkpoint: `config/unit-property-fuzz-coverage.json` now maps the 31.2 unit/property/fuzz categories to existing domain tests and new seed fuzz targets. Added fuzz entrypoints for declarative import hashing, model manifests, webhook payloads, backup restore paths, documentation task packets with untrusted Markdown/path data, and fake-model task-packet extraction. Streamed provider event fuzzing remains explicit remaining work because model-backed workers still request non-streamed completions and no streamed gateway parser is exposed yet.
- [ ] (2026-07-24 10:40Z) Milestone 7 fixture/integration failure checkpoint: `config/integration-failure-coverage.json` mapped the 31.3 fixture repositories and failure-mode inventory to checked proof snippets. The PHP fixture added Vite/Tailwind/npm lock evidence, and provider routing added direct denied-manifest regression coverage for cost-circuit and capability-drift cases. At that checkpoint, provider execution fakes for timeout, rate-limit retry, streamed interruption, and batch partial failure remained explicit unfinished gateway work.
- [ ] (2026-07-24 11:15Z) Milestone 7 local-fake E2E checkpoint: `config/local-fake-e2e-coverage.json` now maps all 14 required local-fake workflow steps to checked implementation, browser, API/CLI, docs, and proof snippets. The deterministic coordinator test now asserts retained evidence-graph nodes and edges for the local publish workflow. `I2-31.4` is in progress because several browser-originated/provider/restore steps are proven by focused tests rather than one continuous browser-to-controller run.
- [ ] (2026-07-24 11:45Z) Milestone 7 performance/soak checkpoint: `config/performance-soak-coverage.json` first mapped scheduler resource safety, runtime benchmark gates, historical evaluation metrics, observability timing/resource records, deterministic acceptance smoke evidence, and then-remaining bounded-soak report work to checked proof snippets. `I2-31.5` stayed in progress because true long-duration soak thresholds and retained pass/fail reports were not yet implemented at that checkpoint.
- [ ] (2026-07-24 12:05Z) Milestone 7 runnable Compose/acceptance checkpoint: `config/compose-acceptance-coverage.json` now maps CI, local acceptance, documented Compose views, mock runtime stack startup/health, and maintenance scripts to checked proof snippets. `I2-DOD-26` is in progress because the full live acceptance script still needs a retained final run after the remaining local-fake E2E, soak, and final-acceptance rows close.
- [ ] (2026-07-24 12:30Z) Milestone 7 bounded soak runner checkpoint: `internal/soak` and `cmd/soak-runner` now produce and validate a retained `local-bounded-soak-v1` JSON report for queue latency p95, scheduler stability/fairness/co-residence, memory growth, provider cost/capability circuit denials, support-bundle size, and local-fake throughput. `scripts/soak.sh` writes `.data/acceptance/soak-report.json` through the supported Compose Go tool container. `I2-31.5` remains in progress because this is a bounded deterministic report, not a final long-duration or full live acceptance run.
- [ ] (2026-07-24 12:35Z) Paused at the requested git sync point after adding a controller-level continuous local-fake acceptance harness. `internal/acceptance/local_fake_e2e_test.go` drives fixture project registration, Repo Doctor proposal acceptance, effective configuration rollback and immutable job snapshotting, provider default-off/allowed/denied/batch fake execution, deterministic workflow progression to the operator gate, backup validation/staged restore/apply, reopened-controller resume, local bare-Git publication, and restored evidence/phase verification. `I2-31.4` remains in progress because this is not yet a real browser E2E and the worker/provider route snapshot plus lower-trust fallback work remains open.
- [ ] (2026-07-24 12:45Z) Milestone 7 provider trust-fallback checkpoint: remote route selection now requires retained successful capability probes for remote models, denies observed model-ID/capability drift from the latest probe, and enforces same-or-higher trust floors before provider enablement, cost, or fallback selection. `TestRemoteRouteDeniesObservedModelDriftFromRetainedProbe` and `TestRemoteRouteRejectsLowerTrustFallbackWhenPrimaryIsUnavailable` close local-fake step 13. `I2-31.4` remains in progress for the real browser E2E and actual worker provider-route snapshot usage.
- [ ] (2026-07-24 13:15Z) Paused at the operator-requested sync point before starting worker provider-route snapshot integration. The branch is synchronized to `origin/dev`; the last implementation checkpoint remains `004c1d8` with passing GitHub Actions run `30095028408`. The remaining `I2-31.4` work is unchanged: real browser E2E plus actual worker provider-route snapshot usage. The newly added `LOCAL_CODE_MAINTAINER_INCREMENT_3_GOAL.md` is recorded as the next-increment contract but does not change the active Increment 2 scope.
- [ ] (2026-07-24 13:45Z) Milestone 7 worker provider-route snapshot checkpoint: implementation, repair, Test Designer, Documentation Agent, and QC worker-call states now retain provider-gateway route decisions before worker execution and store only route/manifest identifiers plus the manifest hash in phase outcomes. Local built-in routes now cover all model-backed worker roles without shadowing explicit fake remote documentation simulations, and default seeding adds missing built-ins to older databases without overwriting operator-edited records. Focused providers/workflow/storage/acceptance tests pass. `I2-31.4` remains in progress for the real browser E2E and editable provider/endpoint/model/route browser approval controls.
- [ ] (2026-07-24 14:10Z) Milestone 7 provider workbench checkpoint: registered provider, endpoint, model, and route profiles now have version-checked update endpoints, generated OpenAPI/TypeScript bindings, and typed Models-page controls. Remote-capable route enablement is rejected unless the controller receives explicit egress-preview approval and rationale, and route updates reject unregistered model references. Focused provider/API Go tests and frontend API drift/tests pass. `I2-31.4` remains in progress for one continuous real browser-to-controller local-fake E2E and per-job remote egress approval when policy requires it.
- [x] (2026-07-24 11:20Z) Milestone 7 unit/property/fuzz closure checkpoint: `internal/providers/stream.go` now owns bounded provider-family streamed event parsing, and `internal/providers/stream_test.go` adds seed fuzz coverage for malformed chunks, interleaved tool-call deltas, cancellation, truncation, and partial usage events. `config/unit-property-fuzz-coverage.json` is all-covered and top-level `I2-31.2` is now implemented. `I2-31.3` remains in progress for outbound provider timeout, rate-limit retry, and batch partial-failure execution fakes.
- [ ] (2026-07-24 11:43Z) Paused at the operator-requested git sync point after the I2-31.2 streamed provider event fuzzing closure checkpoint. Commit `87d2e40` is on `origin/dev`, GitHub Actions run `30090138123` passed, and tracked files were clean before this pause-note-only update. Increment 2 remains active and incomplete; the next implementation boundary is `I2-31.3` provider timeout, rate-limit retry, and batch partial-failure execution fakes.
- [x] (2026-07-24 12:00Z) Milestone 7 fixture/integration failure closure checkpoint: `internal/providers/execution.go` now adds deterministic provider-gateway execution fakes bound to retained allowed route manifests. `internal/providers/execution_test.go` covers timeout retry exhaustion, rate-limit retry-after circuit opening, stream truncation, project-isolated batch enforcement, cross-project batch rejection, and per-item batch partial-failure evidence without network contact. `config/integration-failure-coverage.json` is all-covered and top-level `I2-31.3` is now implemented.
- [x] Milestone 1 — replace the single-document configuration surface with the complete typed registry, seven-scope effective-value resolver, immutable job snapshots, drafts/review/apply/rollback/import/export/dry-run/prerequisite APIs, CLI parity, and dedicated accessible workbench.
- [x] Milestone 2 — add incremental code intelligence, deterministic Context Compiler, baseline/differential verification, test-impact analysis, isolated content-addressed caches, and their status/query/rebuild browser surfaces.
- [x] Milestone 3 — add evidence-backed Repo Doctor onboarding, signed/checksummed capability-pack lifecycle, PHP/R/security packs, and catalog/assignment/upgrade/rollback UI.
- [x] Milestone 4 — normalize forge behavior across existing GitHub, GitLab, and local bare Git; add the constrained simulated Windows worker and heterogeneous fixtures; preserve credential isolation and provider-specific metadata.
- [ ] Milestone 5 — add task contracts, deterministic risk routing and waivers, versioned agent contracts, independent Test Designer, golden/rehearsal gates, Documentation Agent/QC, and OPA-backed policy authoring/simulation/activation/rollback.
- [ ] Milestone 6 — add resource scheduling, the provider-neutral model gateway and safe runtime laboratory, evidence graph, isolated historical evaluation, OpenTelemetry, bounded support bundles, and complete operational pages.
- [ ] Milestone 7 — close every matrix row, run migration/backup/restore/restart and full Increment 1+2 acceptance, complete security/property/fuzz/browser/performance/soak evidence, update operator documentation, and list only genuinely external validations as operator-only.

## Surprises & Discoveries

- Observation: the Increment 2 contract tells Codex to read `LOCAL_CODE_MAINTAINER_GOAL.md`, but that file does not exist in the tree; `project.md` contains the matching completed base-appliance contract and repository guidance names it authoritative.
  Evidence: `git ls-files` and `rg --files` find `project.md` and the two untracked increment goal files, but no `LOCAL_CODE_MAINTAINER_GOAL.md`. This plan therefore treats `project.md` as the base contract without inventing an alias file.
- Observation: Increment 1 configuration is one append-only whole-system JSON document, not a setting registry.
  Evidence: `internal/config/types.go` defines `System`; `config_revisions` stores before/after documents; the API exposes only get, validate, revise, list revisions, and rollback. This is a useful immutable audit backbone but cannot express scoped provenance, drafts, secrets, prerequisites, or optimistic per-scope edits.
- Observation: the existing console has 10 top-level pages and configuration appears only as a revision/rollback table under Administration.
  Evidence: `web/src/OperatorConsole.tsx` defines ten `PageID` values. Increment 2 requires 17 named, navigable operational areas plus typed configuration controls.
- Observation: placing the deterministic Windows simulator in the Windows-worker domain package would make the capability catalog import an implementation through the domain package graph.
  Evidence: moving the adapter to `internal/windowsworker/simulator` leaves `internal/capabilities` dependent only on the pure closed contract while controller composition selects the simulator explicitly.
- Observation: GitHub's repository issues endpoint includes pull requests, while GitLab exposes downloadable artifact metadata on job records rather than through an equivalent bounded project-wide artifact listing.
- Observation: existing model-backed agents still use the internal HTTP v1 model endpoint directly; the safest first provider-gateway seam is durable routing/profile/egress evidence, followed by replacing worker call paths once profile snapshots and conformance adapters are broader.
  Evidence: `internal/agents/runner.go` owns the direct `ModelClient`, while the new `internal/providers` package and migration 35 add controller-visible profile and egress records without giving agents endpoint authority.
  Evidence: fake-server inventory regressions include a GitHub issue carrying `pull_request`, which is excluded in favor of the dedicated pull endpoint, and a GitLab job with `artifacts_file`, which produces a separate normalized artifact record without fetching its archive.
- Observation: the tool containers intentionally use a fresh in-memory `/src/web/node_modules` for each run.
  Evidence: `npm run check:api` alone failed with `openapi-typescript: not found`; `npm ci && npm run check:api` passed. Every recorded frontend/container gate must install the locked dependencies in that invocation.
- Observation: a high-severity `js-yaml` advisory appeared after the clean baseline without any lockfile change, and both `openapi-typescript`'s Redocly 1.x parser and the diff viewer consume the 4.x line.
  Evidence: the first baseline `npm ci` reported zero vulnerabilities; the later audit identified 4.2.0. The official registry publishes patched legacy-line 4.3.0, and a root override deduplicates both consumers to that exact release while leaving the direct tool versions pinned; the rerun reports zero vulnerabilities.
- Observation: migration 16 had already been committed before the export/import persistence extension was implemented.
  Evidence: editing the migration in place would leave any database opened at the intermediate commit recorded at version 16 but missing the new columns and tables. Migration 16 is now byte-identical to its committed form, and migration 17 adds the import structures with a retained-draft upgrade fixture.
- Observation: job submission had three durable creation paths: ordinary/API and Hermes shared `Store.CreateJob`, while scheduled work inserted the job inside the schedule-dispatch transaction.
  Evidence: attaching snapshots only in the HTTP handler or registry service would miss Hermes/schedules or introduce a job-without-snapshot crash window. The configured SQLite store now resolves trusted effective system/project values and inserts the immutable snapshot inside both job-creation transactions.
- Observation: the first workbench implementation asked `/config/effective` for only the selected project/pack/environment layer, while the controller's contextual validator correctly included system state.
  Evidence: non-system browser views could therefore display a built-in value where the accepted job would inherit a system override. The client now requests system plus the selected scope and a regression test covers relation/default previews against the same prospective values.
- Observation: code-intelligence refresh cannot safely accept a worktree path or source payload from the dashboard, and the controller does not own the credential-isolated mirror filesystem.
  Evidence: the existing Git bridge already owns registration, synchronization, mirrors, and credentials. A new exact-revision bounded snapshot method there preserves the boundary; the controller hashes returned bytes and the public refresh request has no body or path field.
- Observation: the existing reproduction phase is the last clean, unmodified worktree seam before implementation, while all accepted jobs already carry an immutable configuration snapshot.
  Evidence: capturing the policy-defined baseline there binds pre-existing observations to the exact base SHA and accepted settings without adding a second workflow authority. Purpose-bound candidate differentials then remain idempotent across targeted, full, and final retries.
- Observation: cache metadata alone cannot prove that a retained derived object remains intact.
  Evidence: parsed-blob verification must reserialize the stored analysis and recompute its cache key, complete input digest, and object digest. Cache kinds without a controller-owned verifier are explicitly unavailable; metadata equality is not treated as integrity evidence.
- Observation: an editable classification or baseline row would destroy the evidence needed to review why policy changed.
  Evidence: migration 20 retains original baseline/differential/impact rows and appends separately attributed supersession, correction, and override records plus audit events. Effective review history can be reconstructed without trusting mutable presentation state.
- Observation: directly linking official Tree-sitter Go grammars into the controller breaks its intentional `CGO_ENABLED=0` distroless build.
  Evidence: the control-plane Dockerfile compiles controller/recovery binaries without CGO. Tree-sitter therefore moved behind a one-file internal parser service; explicit non-CGO controller tests and both image builds prove the boundary.
- Observation: installation version and project assignment version must remain separate to make pinning and upgrade previews truthful.
  Evidence: a global pack upgrade can be compatible for one project and unsafe for another. Migration 21 stores the installed catalog version and each project's exact assignment independently; lifecycle changes never rewrite assignments, and proposal acceptance requires the exact proposed installed version.
- Observation: the controller cannot safely own hosted-forge tokens merely to normalize provider metadata.
  Evidence: the Git bridge already owns credential transport and remote Git operations. The new forge service persists only opaque credential references, status, normalized objects, cursors, and provider metadata; GitHub App and GitLab tokens remain confined to the bridge.
- Observation: the requested GitHub destination existed but had no commits or default branch.
  Evidence: authenticated `gh repo view B1-Mordred/CodeMaintainer` reported administrator access and an empty `defaultBranchRef`; `git ls-remote --symref` returned no refs. Initial synchronization can therefore preserve the complete local `dev` history without force-pushing or reconciling unrelated commits.
- Observation: the specialized R and security pack browser used one free-form JSON textarea even though the controller previously checked only JSON syntax.
  Evidence: `CapabilityPage.tsx` parsed arbitrary proposal JSON client-side and assignment updates had no typed route. The controller now derives both editors and normalization from the trusted manifest, rejects duplicate/unknown/nested undeclared fields and unsafe values, and proves preview/apply/restart parity through UI, API, CLI, and storage tests.
- Observation: the original full acceptance script is not repeatable while the retained pre-rename appliance owns `127.0.0.1:8080` and its CLI session has expired.
  Evidence: the first live run stopped on the occupied port and the alternate-port run reached a healthy controller but `maintainctl doctor` returned 401 against retained state. A Compose `!override` used `127.0.0.1:18080` and a disposable isolated data/session root; fresh bootstrap, doctor, and desktop/mobile Chromium passed, the temporary stack/data were removed, and the retained `local-code-maintainer` health endpoint remained green.
- Observation: embedded OPA was feasible without introducing a sidecar or controller-owned policy execution daemon.
  Evidence: the controller uses `github.com/open-policy-agent/opa/v1/format` and `github.com/open-policy-agent/opa/v1/rego`, pins the dependency graph in `go.mod`/`go.sum`, and the full Go, race, vet, frontend, OpenAPI, and Compose checkpoint passed after the addition.
- Observation: Test Designer reports were append-only but their proposal dispositions were embedded in the original report, leaving no safe way to record later reviewer decisions.
  Evidence: migration 27 stored only `test_designer_reports.proposals`; adding migration 32 keeps the report immutable and derives effective dispositions from the latest append-only reviewer record.
- Observation: strict JSON Schema response-format requests are only a model hint; trusted controller validation still catches wrong job bindings and domain invariants that a generic schema cannot express.
  Evidence: the retry regression first returns a syntactically valid QC payload bound to the wrong job. `DecodeQCReport` rejects it, the runner sends bounded validation feedback, and the second exact-bound payload is accepted.
- Observation: a changed golden report previously returned an error from `golden_rehearsal_review`, which made the engine persist `failed` rather than an operator-actionable gate.
  Evidence: `reviewGoldens` returned `errors.New("golden rehearsal gate requires explicit approval before QC")` for `approval_required`; the new state test and API test prove legal routing to `awaiting_golden_approval` and then to `loading_documentation_model` after approvals.
- Observation: golden tolerance and mask changes were only static JSON embedded in immutable report comparisons.
  Evidence: adding migration 33 stores reviewer/admin `golden_comparison_profiles` separately from `golden_reports` and `golden_approvals`; the API and CLI tests record a typed profile without rewriting the original report.
- Observation: blocked documentation manifests failed the workflow without producing retained findings for the operator to resolve or waive.
  Evidence: `reviewDocumentation` previously returned an error on `blocked` manifests after saving the manifest; the new workflow test proves failed checks, pending requirements, and unsupported claims become stable `QC-DOC*` findings.
- Observation: Documentation Agent policy selection was implicit in worker/manifests rather than previewable before a job reached documentation review.
  Evidence: before this checkpoint `maintainctl documentation` and `/jobs/{jobID}/documentation` listed only retained manifests. The new profile and simulation routes expose the active rule set and predicted required docs/checks/render targets from bounded change evidence.
- Observation: the durable schedule/queue path had retained runs and serial dispatch, but no auditable resource decision record.
  Evidence: schedule history recorded dispatch or window skip, while the new scheduler migration separately stores selected/deferred/rejected job IDs, co-residence safety, fairness application, batch grouping, and resource rationale for each simulation.
- Observation: live lease acquisition was the narrowest safe enforcement seam for scheduler co-residence policy.
  Evidence: `AcquireJobLease` already owns the transaction that selects resumable work and writes `job_leases`; moving scheduler simulation into that transaction lets selected decisions and lease ownership commit together without adding a second workflow authority.
- Observation: schema evolution had many migration files and some historical restart tests, but no machine-readable proof that each migration carried backup/recovery and rollback evidence.
  Evidence: migrations existed through `040_observability_support.sql`, `store_test.go` asserted selected tables only through migration 38, and `config/increment-2-coverage.json` still marked `I2-27`, `I2-DOD-02`, and `I2-DOD-25` missing. The new `config/migration-coverage.json` and `migration_coverage_test.go` force every embedded migration/file pair to carry durable-record, restart/resume, backup/restore, documentation, and rollback entries.
- Observation: operator-only validation procedures were documented but not tied to the fake evidence that CI actually proves.
  Evidence: `docs/acceptance.md` listed eight operator-only checks, while no checked artifact mapped those checks to fake services/tests or recorded the non-claim boundary. The new external-validation inventory and config test require every external area to have prerequisites, fake evidence, operator procedure paths, and a CI boundary explanation.
- Observation: descriptor-level configuration coverage did not prove operation-level UI/API/CLI parity.
  Evidence: `configuration-coverage.json` mapped every setting to surfaces, but it did not enumerate the registry operations themselves or compare them to OpenAPI and `maintainctl`. The new parity matrix covers descriptors, values, effective resolution, prerequisites, export/import, the full draft lifecycle, registry history/rollback, and legacy compatibility export/validate/apply/rollback.
- Observation: the cleanest pause point was before starting the redaction inventory.
  Evidence: the tracked worktree matched `origin/dev` at `f6d25b6`, GitHub Actions run `30076306125` was green, and the next `I2-DOD-07` slice crosses configuration, provider egress, observability/support bundles, memory, auth, and Windows-worker credential handling.
- Observation: redaction behavior existed in multiple isolated packages, but no checked artifact proved the surfaces moved together.
  Evidence: configuration, provider egress, observability, memory, auth, forge, Windows-worker, and policy tests already asserted local leak-prevention behavior; `I2-DOD-07` was still `missing` because no matrix connected those proofs to the DOD boundary.
- Observation: Milestone 2 intelligence behavior was implemented but DOD-11 was still unclaimed at the final matrix layer.
  Evidence: the service, workflow, API, CLI, and browser tests already covered indexing, context manifests, baselines, differentials, impact, and caches; `config/increment-2-coverage.json` still marked `I2-DOD-11` as `missing` before the checked intelligence matrix connected those proofs.
- Observation: DOD-13 spans intelligence, workflow, golden/rehearsal, API, CLI, and browser code, so no single existing test represented the complete acceptance boundary.
  Evidence: service tests reject classifying a newly introduced finding away, workflow code refuses final verification without full-suite impact evidence, golden tests require approval status, and browser/API/CLI code exposes the operations; `config/increment-2-coverage.json` still marked `I2-DOD-13` as `missing`.
- Observation: DOD-14 had substantial implementation evidence but also known remaining scope, so a binary implemented/missing row was hiding the useful boundary.
  Evidence: the documentation policy profile, simulation API/CLI/UI, Documentation Agent manifest binding, blocked-finding conversion, fresh documentation verification, and retained manifest surfaces exist; persistent editable policy profiles, renderer/tool profile management, rich render previews, and independent documentation QC actions remain explicit work.
- Observation: DOD-23 security/adversarial evidence was broad but scattered across package tests.
  Evidence: authorization, request-boundary, runnerd, provider, webhook, memory, evaluation, backup, capability-preview, and redaction regressions already existed, but `config/increment-2-coverage.json` still marked `I2-28` and `I2-DOD-23` missing until the security/adversarial matrix tied those proofs together.
- Observation: Increment 2 had no Go fuzz targets before the 31.2 checkpoint.
  Evidence: `rg "func Fuzz"` returned no tests, even though the contract requires property/fuzz coverage for imports, manifests, webhooks, paths, archives, tool calls, and untrusted Markdown.
- Observation: streamed-provider-event fuzzing could not be claimed before this checkpoint because no streamed chunk parser existed.
  Evidence: the model worker still sends `"stream": false` and the fake model server rejects streaming requests, but `internal/providers/stream.go` now provides a gateway-owned parser to fuzz before worker traffic moves through provider route snapshots.
- Observation: the PHP fixture did not fully represent the 31.3 PHP/Vite/Tailwind fixture requirement before this checkpoint.
  Evidence: `test/fixtures/repositories/php83` had Composer, PHPUnit, migrations, and GitLab CI files, while `vite.config.*`, `tailwind.config.*`, and an npm lockfile were absent.
- Observation: provider-family probes, streamed parsing, and deterministic execution fakes prove different provider-gateway failure layers.
  Evidence: all required provider families have default fake profiles and retained probes; `internal/providers/stream.go` handles bounded streamed chunk parsing; `internal/providers/execution.go` now binds timeout, rate-limit, stream-truncation, and project-isolated batch partial-failure fakes to retained allowed egress manifests without contacting external providers.
- Observation: the deterministic coordinator E2E covers the repair/publication core and now proves provider route snapshots for model-backed worker calls, but not every browser-originated local-fake contract step as one real browser flow.
  Evidence: `TestCoordinatorCompletesImplementRejectRepairApproveAndLocalPublish` drives task approval, risk/context/test impact, deterministic implementation, Test Designer, verification, documentation, QC block/repair, local bare-Git publication, artifacts, memory, evidence graph checks, retained provider egress manifests, and phase outcome `provider_egress_manifest_sha256` evidence for implementation, repair, Test Designer, Documentation Agent, and QC worker calls. Browser onboarding, pack acceptance/effective-config inspection, UI configuration rollback, fake remote route approval, lower-trust fallback, and backup/migrate/restore/resume are currently proven by focused UI/API/CLI/storage tests and are tracked as remaining work in `config/local-fake-e2e-coverage.json`.
- Observation: restored publication resume needs volatile Git bridge registrations rebuilt from durable project state.
  Evidence: the local-fake restore harness reopens the SQLite controller after `backup.ApplyPending`; without re-registering the local project with the in-memory Git bridge manager, publication fails request validation even though the durable project and job state are intact.
- Observation: remote provider fallback must establish its trust floor before checking provider availability or cost.
  Evidence: before this checkpoint a disabled or drifted approved-private primary could fall through to a public-remote model later in the same ordered route. `SimulateRoute` now establishes the first eligible model's trust tier before enablement/cost checks, requires the latest retained remote capability probe to match the pinned model ID and capabilities, and records denied egress manifests for drift or lower-trust fallback.
- Observation: performance and soak evidence first existed in scheduler, runtime benchmark, evaluation, and observability domains before the bounded report runner was added.
  Evidence: scheduler tests prove bounded resource/fairness decisions, runtime benchmarks retain speed/memory/quality identity, evaluation records wall/CPU/memory/cache metrics, and observability retains duration/queue/resource facts. `config/performance-soak-coverage.json` recorded those proofs before the later `local-bounded-soak-v1` runner checkpoint.
- Observation: the bounded soak runner can be deterministic and still remain distinct from long-duration production soak evidence.
  Evidence: `internal/soak` generates a self-validating `local-bounded-soak-v1` report with fixed iteration bounds, SHA-256 report identity, threshold checks, fake provider cost/capability circuit denials, and a 256 KiB artifact cap. `docs/performance-soak.md` continues to state that no current CI job claims a long-duration soak pass.
- Observation: the runnable-stack evidence was split between CI, `scripts/acceptance.sh`, Compose docs, and maintenance scripts.
  Evidence: GitHub CI validates race tests, frontend build, dev Compose config, and control-plane image builds; `scripts/acceptance.sh` additionally bootstraps, starts the mock controller/runnerd, health-checks, runs `maintainctl doctor`, and executes browser acceptance. `config/compose-acceptance-coverage.json` now ties those proofs to `I2-DOD-26`.

## Decision Log

- Decision: preserve `config_revisions` and build an additive registry/scoped-value/draft/snapshot model around it.
  Rationale: the existing append-only revision ledger, diff, audit, rollback, backup, and restore behavior is proven Increment 1 functionality. Additive migration avoids destructive conversion and lets the legacy whole-system document remain a compatibility projection while new code uses typed descriptors and effective values.
  Date/Author: 2026-07-20 / Codex
- Decision: use stable dotted setting keys and JSON-compatible typed values, with descriptors compiled into the trusted controller binary and mutable values stored outside worktrees.
  Rationale: compiled descriptors make type, scope, role, secret, dependency, apply, and dry-run behavior deterministic and reviewable. Values can be exported and migrated without allowing repository content to define trusted policy or executable hooks.
  Date/Author: 2026-07-20 / Codex
- Decision: implement Increment 2 by domain slices that include schema, storage, service, API/OpenAPI, CLI, UI, authorization, audit, tests, and docs before a slice is marked complete.
  Rationale: the contract explicitly forbids deferring the browser or configuration surface to a cosmetic final phase. Vertical slices keep API/CLI/UI behavior aligned and make the coverage matrix enforceable.
  Date/Author: 2026-07-20 / Codex
- Decision: use deterministic in-process or loopback fakes for every external family, and keep real adapters disabled until an administrator explicitly creates and probes an approved profile.
  Rationale: CI must prove protocols, redaction, SSRF defenses, capability negotiation, fallbacks, costs, retries, and failure closure without contacting real providers or requiring secrets.
  Date/Author: 2026-07-20 / Codex
- Decision: keep code intelligence controller-local and project-separated in SQLite, with derived blobs keyed by repository/blob/tool identity.
  Rationale: this matches the single-host deployment, existing durable store, backup/recovery model, and namespace boundary. Tree-sitter/SCIP/read-only LSP adapters can enrich data without becoming workflow authorities or receiving unrestricted execution.
  Date/Author: 2026-07-20 / Codex
- Decision: preserve unknown declarative-import entries only inside immutable import-draft evidence and never copy them into effective scoped values.
  Rationale: forward compatibility must retain future keys losslessly enough for review without granting unregistered data any runtime authority. Known keys still pass current descriptor validation, and strict mode rejects every unknown key.
  Date/Author: 2026-07-20 / Codex
- Decision: pin a root npm override from vulnerable `js-yaml` 4.2.0 to the patched compatible 4.3.0 release.
  Rationale: the current pinned `openapi-typescript` release fixes its Redocly 1.x parser dependency at 4.2.0 and no newer generator release exists. The narrow override removes the reachable advisory without adopting an incompatible js-yaml 5.x API or floating any production dependency.
  Date/Author: 2026-07-20 / Codex
- Decision: install the compiled registry into the SQLite composition root and create accepted-job snapshots inside the same transaction as the job and initial transition.
  Rationale: the store is the one shared boundary for API and Hermes submissions and owns the schedule insertion transaction. Using only the trusted compiled registry plus exact stored system/project scopes keeps resolution deterministic, makes snapshot failure abort acceptance, and avoids a second connection/deadlock or post-commit crash window.
  Date/Author: 2026-07-20 / Codex
- Decision: make descriptor relations optionally conditional on the owning setting's typed value, and execute prerequisite/dry-run names only through controller-compiled checker functions.
  Rationale: a dependency such as waiver-rationale policy is active only while human waivers are enabled. Typed activation avoids rejecting a safe joint disable, while compiled checker registration keeps imported/browser text from becoming executable authority and reports unknown handlers as unavailable rather than passed.
  Date/Author: 2026-07-20 / Codex
- Decision: key reusable syntax analysis by project, registered repository, blob SHA-256, and parser identity, while obtaining source only through an exact-commit Git-bridge snapshot.
  Rationale: blob identity provides cross-branch reuse without permitting cross-project evidence leakage. The browser cannot upload code, select a host path, or invoke a parser executable; unsupported and malformed inputs remain explicit partial evidence.
  Date/Author: 2026-07-20 / Codex
- Decision: capture workflow baselines before implementation and bind them to exact base revision, accepted configuration hash, verifier identity, and pack-set hash; key differentials additionally by candidate patch and verification purpose.
  Rationale: the identity is strong enough for restart replay without silently conflating targeted, full, and final evidence. The controller, not an agent, creates and compares these records, and the final state gate checks test-impact policy before advancing.
  Date/Author: 2026-07-20 / Codex
- Decision: implement cache warming as an alias of the existing trusted exact-snapshot refresh path and make verification/simulation read-only typed operations.
  Rationale: warming must not introduce a second source-ingestion boundary, while verification must recompute controller-owned objects and simulation must not reserve capacity or evict unrelated project evidence.
  Date/Author: 2026-07-21 / Codex
- Decision: make DOD-11 intelligence evidence a checked matrix rather than relying on milestone prose.
  Rationale: indexing, context selection, baseline/differential review, impact analysis, and cache controls span multiple subsystems. A single machine-checked inventory keeps the final acceptance boundary explicit without reworking already-covered Milestone 2 implementation code.
  Date/Author: 2026-07-24 / Codex
- Decision: make DOD-13 a checked safety matrix across differential, final verification, and golden approval gates.
  Rationale: the requirement combines three separate non-waiver guarantees. Recording implementation, tests, UI, docs, and proof snippets together prevents a future feature from weakening one safety gate while leaving the others apparently green.
  Date/Author: 2026-07-24 / Codex
- Decision: make DOD-14 an in-progress checked matrix with explicit known remaining work.
  Rationale: documentation policy crosses the documentation agent, workflow coordinator, API, CLI, browser, finding lifecycle, and source-controlled verification. A checked matrix proves current behavior without overstating the unfinished renderer/tool-profile and independent documentation-QC scope.
  Date/Author: 2026-07-24 / Codex
- Decision: treat Increment 2 security/adversarial acceptance as a checked cross-surface inventory before final acceptance.
  Rationale: no single package owns authorization, injection, isolation, SSRF, webhook, restore, preview, and redaction behavior. A machine-readable matrix lets CI fail when a boundary loses implementation, test, documentation, or proof evidence without falsely claiming final acceptance is complete.
  Date/Author: 2026-07-24 / Codex
- Decision: advance I2-31.2 as in-progress with real fuzz targets and an explicit remaining streamed-events item.
  Rationale: seed fuzz functions now exercise implemented untrusted parsers during normal Go tests and can be expanded with `go test -fuzz`, but a streamed-provider parser must exist before malformed streamed-event fuzzing can prove anything meaningful.
  Date/Author: 2026-07-24 / Codex
- Decision: advance I2-31.3 as in-progress with a checked fixture/failure evidence matrix rather than marking it complete.
  Rationale: fixture repositories and many integration failure modes are now machine-checked against real files and tests, but remote-provider timeout/rate-limit/stream/batch behavior cannot be fully proven until outbound provider execution exists behind the gateway.
  Date/Author: 2026-07-24 / Codex
- Decision: close I2-31.3 with deterministic provider-gateway execution fakes rather than real remote transport calls.
  Rationale: timeout retry exhaustion, rate-limit retry-after handling, stream interruption, and batch partial failure are acceptance failure modes that can be proven safely by a controller-owned fake after route selection and manifest retention. Real protocol adapters and worker route snapshots remain separate Milestone 6 provider work.
  Date/Author: 2026-07-24 / Codex
- Decision: model evidence corrections, baseline updates, and targeted-test overrides as append-only review ledgers, with replacement baselines derived only from stored candidate observations.
  Rationale: original evidence stays reviewable; privileged changes are explicit, attributed, reasoned, and audited; and neither a correction nor an inner-loop override can weaken the newly-introduced-failure or fresh-full-suite safety invariants.
  Date/Author: 2026-07-21 / Codex
- Decision: isolate pinned CGO Tree-sitter grammars in a non-root internal service and keep the controller-side analyzer protocol hash-bound, one-file-at-a-time, and fact-validated.
  Rationale: real multi-language syntax parsing must not weaken the static controller image or create an arbitrary workspace/LSP execution channel. SCIP/LSP facts use the same bounded read-only enrichment contract.
  Date/Author: 2026-07-21 / Codex
- Decision: compile typed capability manifests into the trusted controller catalog, checksum their canonical content, and permit only registered IDs rather than pack-supplied executable extension fields.
  Rationale: the product needs rich reusable detection, verification, policy, UI, and rehearsal metadata without allowing a pack, repository, or browser to select images, commands, mounts, networks, capabilities, or code. Independently reviewed executable plugins remain a separate lifecycle rather than an implicit pack feature.
  Date/Author: 2026-07-21 / Codex
- Decision: make Repo Doctor a trusted-snapshot evidence compiler whose scan and review transactions are deliberately separate.
  Rationale: detection can be retried and compared safely only if it has no configuration side effect. Explicit optimistic review binds a human reason to the exact proposal version, while exact-version pack assignment and audit commit atomically.
  Date/Author: 2026-07-21 / Codex
- Decision: expose hosted and local repository integrations through a normalized `ForgeProvider` boundary while retaining provider-specific metadata and explicit unsupported/operator-only capabilities.
  Rationale: controller workflows need stable object, cursor, retry, and idempotency semantics without importing provider SDKs or receiving credentials. Explicit capability states prevent a local bare repository or a permission-limited hosted installation from appearing feature-complete.
  Date/Author: 2026-07-21 / Codex
- Decision: encode hosted inventory continuation as a bounded versioned cursor containing only the synchronized base SHA, endpoint index, and provider page, and retry only idempotent GET requests on 502/503/504.
  Rationale: the cursor resumes large provider inventories without exposing tokens or mutable URLs, resets safely when the Git base changes, and avoids repeating publication or other state-changing calls. Rate limits return durable partial state and retry delay instead of sleeping inside a controller request.
  Date/Author: 2026-07-22 / Codex
- Decision: represent Windows work as ten fixed operation classes over a closed immutable-input schema, with the deterministic simulator in a separate adapter package and remote credentials represented only by opaque profile references.
  Rationale: .NET, PowerShell, service, installer, HAMILTON, VPN, release, IQ, equipment, and signing evidence can be tested without creating an arbitrary PowerShell or host-control endpoint. Domain callers remain implementation-independent, controller policy owns manual gates, and CI needs no Windows host or proprietary equipment.
  Date/Author: 2026-07-22 / Codex
- Decision: resolve a remote Windows worker credential only from an operator-mounted fixed secret directory using a validated opaque profile reference, and never fall back to the simulator when remote routing fails.
  Rationale: the API remains unable to supply a host path or raw credential, profile updates can preserve an existing write-only binding, and an unavailable or unauthenticated physical worker fails closed instead of returning misleading simulated evidence.
  Date/Author: 2026-07-22 / Codex
- Decision: rename the product and canonical Go module to `CodeMaintainer` and `github.com/B1-Mordred/CodeMaintainer`, but retain compatibility-sensitive operator and persistence identifiers.
  Rationale: branding, imports, package metadata, Compose identity, schema namespaces, diagnostics, and documentation should match the canonical repository. Existing `MAINTAINER_*` environment variables, executable names, database schema, contract filenames, and the original backup encryption associated-data value remain stable so the repository rename does not invalidate deployments or backups.
  Date/Author: 2026-07-21 / Codex
- Decision: make each capability manifest's bounded UI schema the single controller-owned configuration contract for proposal acceptance and later assignment changes.
  Rationale: a generated typed editor alone cannot enforce authority. Normalizing defaults and rejecting every unregistered field in the service before the optimistic storage transaction keeps browser, REST, CLI, restart state, and audit behavior identical without exposing commands, paths, scanner arguments, or arbitrary policy input.
  Date/Author: 2026-07-22 / Codex
- Decision: route only medium/high-risk work through the independent Test Designer after full candidate verification, while recording low-risk skips as bounded evidence if the state is explicitly reached.
  Rationale: risk remains a deterministic controller decision, Test Designer proposals bind to an exact task contract and result SHA, and low-risk work does not acquire an unnecessary model dependency or implicit quality gate.
  Date/Author: 2026-07-23 / Codex
- Decision: represent golden changes as append-only comparison and approval evidence, never as automatic baseline rewrites during workflow execution.
  Rationale: a failing or changed golden is authority-bearing release evidence. The controller can record candidate-vs-approved identities and require an authenticated reasoned decision, but updating the approved artifact must remain a separate policy-governed act rather than a convenient response to failure.
  Date/Author: 2026-07-23 / Codex
- Decision: if the Documentation Agent changes source-controlled documentation, commit it as ordinary repository content and rerun exact-commit verification before QC.
  Rationale: documentation changes are reviewed code changes and can affect release evidence, generated references, links, or examples. Updating `result_sha`, recording test-impact evidence, and verifying the documentation commit prevents QC from reviewing a commit that did not pass the required deterministic gates.
  Date/Author: 2026-07-23 / Codex
- Decision: implement advanced policy bundles with the embedded OPA Go library and require retained tests for the exact formatted source before activation.
  Rationale: this keeps policy evaluation inside the controller's transactional/auditable boundary without exposing a new network sidecar, while source formatting plus source hashes prevent a tested bundle from drifting before activation. OPA failures still produce fail-closed decisions.
  Date/Author: 2026-07-24 / Codex
- Decision: pause jobs with required pending Test Designer proposals in a dedicated non-resumable wait state and record dispositions in a separate append-only ledger.
  Rationale: advancing into golden/docs/QC with pending independent recommendations weakens the quality gate, while mutating the original report would erase evidence. A dedicated state makes operator attention explicit, and latest-record overlays allow corrections without hiding history.
  Date/Author: 2026-07-24 / Codex
- Decision: perform structured-output retry inside the trusted model-worker runner before applying edits or storing trusted agent artifacts.
  Rationale: malformed model output should get a bounded correction opportunity, but unsafe edits or unsupported claims must not become durable trusted evidence. Keeping retries inside the runner preserves the controller's fixed worker contract while ensuring only validated final JSON reaches downstream workflow code.
  Date/Author: 2026-07-24 / Codex
- Decision: treat golden update approval as a durable wait state with latest append-only decision resolution rather than a terminal failure or mutable report update.
  Rationale: changed goldens are operator decisions, not model or workflow failures. A wait state preserves forward progress and auditability while still preventing documentation, policy, QC, or publication from proceeding until every required comparison has an explicit approved decision.
  Date/Author: 2026-07-24 / Codex
- Decision: keep golden tolerance/mask profile changes append-only, typed, and separate from the report and approval ledgers.
  Rationale: tolerance and mask adjustments are review evidence, not report generation output or baseline rewrites. A separate typed ledger lets reviewers document allowed variance while preserving immutable candidate-vs-approved artifact evidence and the independent approval decision.
  Date/Author: 2026-07-24 / Codex
- Decision: reuse the existing QC finding lifecycle for documentation-policy blockers instead of adding a parallel documentation-only resolution table.
  Rationale: Increment 2 requires blocking documentation findings to follow the same state machine as code findings. Converting manifest evidence into stable finding records preserves one audited resolution/waiver path while keeping documentation manifests append-only.
  Date/Author: 2026-07-24 / Codex
- Decision: expose documentation impact simulation as a controller-owned typed policy profile before adding persistent editable profile storage.
  Rationale: operators need previewable change-to-document impact now, but policy editing is authority-bearing and should arrive with its own versioned persistence and activation semantics. A built-in typed profile gives API/CLI/browser parity without accepting raw commands, tool names, or repository-supplied policy code.
  Date/Author: 2026-07-24 / Codex
- Decision: keep the first resource scheduler checkpoint advisory and append-only rather than changing live lease semantics.
  Rationale: the existing serial dispatcher is already durable and safe for current mock operation. Recording deterministic scheduling reasons first creates the audit/control surface needed for operator validation without risking a partial queue-leasing rewrite at a pause boundary.
  Date/Author: 2026-07-24 / Codex
- Decision: enforce scheduler decisions at job lease acquisition while continuing to use the existing workflow state machine.
  Rationale: lease acquisition is where concurrent controllers/workers can otherwise co-reside unsafe work. Keeping phase transitions unchanged preserves the controller as sole workflow-state authority while making every selected live lease resource-checked and auditable.
  Date/Author: 2026-07-24 / Codex
- Decision: introduce the provider gateway as controller-owned routing and egress-manifest evidence before moving every model-backed worker call through it.
  Rationale: profile, endpoint, data-class, fallback, budget, and SSRF policy can be validated and tested without contacting real providers or destabilizing the existing local fake model workflow. The remaining worker integration will bind exact provider route snapshots once the adapter conformance layer is present.
  Date/Author: 2026-07-24 / Codex
- Decision: bind provider route snapshots at the workflow worker-call states, not only the preceding model-loading states.
  Rationale: implementation, repair, Test Designer, Documentation Agent, and QC calls need exact per-call route evidence. Recording only route and manifest identifiers in phase outcomes keeps workers free of credentials, endpoint URLs, and reusable egress authority while the append-only provider manifest store carries the full policy evidence.
  Date/Author: 2026-07-24 / Codex
- Decision: enforce migration evidence through a checked JSON inventory instead of relying only on prose docs or a hardcoded migration count.
  Rationale: Increment 2 requires migrations, backup guidance, idempotent restart/retry behavior, and rollback instructions to stay aligned. A machine-readable inventory makes a new migration fail tests until its durable records, restart/resume evidence, backup/restore evidence, docs, and rollback procedure are named.
  Date/Author: 2026-07-24 / Codex
- Decision: maintain external-only validation as a checked coverage inventory separate from the deterministic CI gate.
  Rationale: real rootless-worker topology, licensed model weights, GitHub installation, OpenViking embeddings, Hermes, remote auth/TLS, separate-host restore, and OCI scanning cannot be honestly claimed by CI. A separate inventory keeps fakes and operator procedures explicit without weakening the local gate.
  Date/Author: 2026-07-24 / Codex
- Decision: add an operation-level configuration parity matrix in addition to descriptor-level setting coverage.
  Rationale: every setting can have metadata while a routine workflow is still unreachable from one surface. Checking routes, CLI commands, browser files, docs, and regression evidence for each operation keeps effective configuration parity auditable without hand-inspecting the workbench.
  Date/Author: 2026-07-24 / Codex
- Decision: pause before implementing the cross-surface secret-redaction matrix.
  Rationale: redaction is a broad safety invariant and should land as a complete tested slice, not as partial uncommitted work after a green configuration-parity checkpoint.
  Date/Author: 2026-07-24 / Codex
- Decision: make redaction coverage a checked inventory with proof snippets instead of a prose-only security claim.
  Rationale: a broad "secret-safe" statement is too easy to drift. Requiring every secret-bearing output surface to reference implementation, UI, tests, docs, forbidden examples, and exact regression markers keeps the DOD tied to executable evidence.
  Date/Author: 2026-07-24 / Codex
- Decision: advance I2-31.4 as an in-progress checked 14-step matrix rather than claiming a complete end-to-end browser workflow.
  Rationale: the current evidence is useful and regression-checked, but it is split across deterministic coordinator, browser, API, CLI, provider, configuration, and backup tests. The matrix prevents drift while preserving honest remaining work for the single local-fake workflow that must drive the whole contract.
  Date/Author: 2026-07-24 / Codex
- Decision: advance I2-31.5 as an in-progress checked performance/soak matrix, not as a completed soak claim.
  Rationale: current deterministic evidence covers bounded resource decisions and metrics capture, but final acceptance requires a repeatable soak runner with pass/fail thresholds and retained reports. The checked matrix keeps existing proof useful while preventing CI smoke from being mistaken for long-duration soak validation.
  Date/Author: 2026-07-24 / Codex
- Decision: advance I2-DOD-26 as an in-progress checked runnable-stack matrix rather than a final live acceptance claim.
  Rationale: CI and scripts prove substantial runnable-stack behavior, but the full live acceptance script can conflict with an existing retained appliance on port 8080 and should be rerun with retained final evidence after the remaining acceptance rows close.
  Date/Author: 2026-07-24 / Codex
- Decision: cover the bounded soak report sub-row with a deterministic short runner, while leaving I2-31.5 open for final long-duration/live acceptance evidence.
  Rationale: queue latency, scheduler, observability, provider-denial, support-bundle, and throughput thresholds now produce a retained JSON artifact. Restart/resume during a soak window and final full-run retention are still broader acceptance work and must not be implied by the short runner.
  Date/Author: 2026-07-24 / Codex
- Decision: close I2-31.2 after adding a bounded gateway-owned streamed event parser and fuzz seeds.
  Rationale: malformed chunks, interleaved tool-call deltas, cancellation, truncation, and partial usage events now have deterministic parser behavior and fuzz entrypoints. This does not imply worker streaming integration or outbound provider execution fakes.
  Date/Author: 2026-07-24 / Codex

## Outcomes & Retrospective

Increment 2 Milestone 1 is complete without removing the proven Increment 1 document compatibility path. The controller now owns typed, scoped, versioned, audited configuration through browser/API/CLI parity, binds complete effective snapshots atomically to accepted jobs, and fails closed on dependencies and trusted readiness checks. Automated unit, API, UI accessibility, build, OpenAPI, and Compose gates pass; the unavailable host Playwright CLI is recorded precisely for the final browser acceptance phase rather than represented as passing evidence.

Milestone 2 is complete. It combines exact incremental indexing and real pinned multi-language syntax adapters, a safe SCIP/read-only-LSP evidence seam, production context selection across all controller-authorized evidence families, pre-change baselines and purpose-bound differentials, impact history and mandatory final full-suite enforcement, verified quota/retention caches, append-only review ledgers, and browser/API/CLI parity. The controller remains the sole workflow authority and retains its static non-CGO image.

Milestone 4 is complete. GitHub, GitLab, and local bare Git share a bounded normalized forge model while credentials remain isolated in the Git bridge; the Windows pack uses ten immutable operation classes through a deterministic simulator or fail-closed authenticated remote adapter; heterogeneous fixtures and migration/restart/failure suites are durable. R and security assignments now use controller-manifest-driven safe editors and closed normalization rather than raw JSON. The full local gate, image builds, authenticated health/doctor, and isolated desktop/mobile Chromium acceptance pass without interrupting the retained pre-rename stack.

Milestone 3 is complete. Repository onboarding is evidence-backed and no-write until an operator explicitly reviews a versioned proposal. Capability packs are canonical, checksummed, prerequisite-visible, pinned/reversible, independently assigned per project, and unable to expand runner authority. The PHP, R, and security packs and their rehearsal definitions are available through the same typed browser/API/CLI service; full golden artifact policy remains intentionally in Milestone 5.

Milestone 6 has started but remains open. The scheduler now has a pure domain simulator, safe built-in topology/profile defaults, retained append-only decisions, API/CLI/browser parity for status, simulations, and history, and transactional live lease enforcement for fairness and declared CPU/RAM co-residence. The provider gateway now has durable versioned profiles, default-off remote templates for every required provider family, endpoint/data/budget/capability route checks, retained egress manifests, append-only deterministic fake capability probes, API/CLI/browser parity, safe local routing evidence, and workflow-bound route snapshots for implementation, repair, Test Designer, Documentation Agent, and QC worker calls. The runtime benchmark lab now retains exact local profile identity, smoke quality/determinism gates, speed, memory, cache eligibility, and non-activating recommendations. The evidence graph now retains typed append-only job/artifact nodes and produced edges through API/CLI/browser access. The historical evaluation lab now retains immutable datasets and isolated offline comparison reports with `eval://memory/...` and `eval://cache/...` namespaces. The observability foundation now retains append-only redacted local telemetry events and bounded support bundle manifests with API/CLI/browser/docs coverage while external OTLP stays disabled by default. Real protocol adapters, provider-native streaming/cancellation/batch/usage reconciliation, runtime activation/rollback, full request-through-publication/SBOM graph coverage, real historical repair execution, and full cross-component OpenTelemetry instrumentation remain before the milestone can close.

Milestone 5 is partially implemented and paused at an auditable sync point. Task contracts, deterministic risk routing, schema-constrained agent contracts with bounded validation-feedback retries, validation evidence, independent Test Designer proposal capture and disposition enforcement, golden/rehearsal report plus non-terminal approval-wait and typed tolerance/mask profile evidence, Documentation Agent manifest, documentation-policy finding, and documentation impact-simulation evidence, deterministic policy-review decisions, and embedded OPA/Rego test-gated policy bundles are durable and visible across API/CLI/browser surfaces; remaining quality policy work is deliberately unclaimed.

Milestone 7 has started but remains open. The browser information architecture now exposes the required 17 named areas with tested top-level navigation, dedicated Documentation and Security/SBOM operational pages, retained setup/health/administration operations, filtered configuration routes from every non-configuration area, reciprocal configuration-field consumer backlinks, and dashboard operator documentation. The migration/restore evidence gate now inventories all 40 embedded forward migrations and verifies that each has durable-record, restart/resume, backup/restore, documentation, and rollback evidence, while backup restore tests cover preserved pre-restore databases and WAL/SHM cleanup. External-only validation now has a checked inventory tying each unavailable real prerequisite to safe fake evidence, finite operator procedures, and explicit CI non-claims. Configuration parity now has a checked operation matrix for REST/OpenAPI, CLI, browser, docs, and tests across typed registry and legacy compatibility operations. Full keyboard/browser E2E, broader migration-from-retained-version acceptance, remaining local-fake E2E, long-duration soak evidence, and final acceptance reporting remain before Milestone 7 can close.

Current checkpoint: I2-31.4 worker provider-route snapshot usage and the typed provider workbench are implemented on top of the controller-level local-fake acceptance harness. The next safe implementation boundary is turning the harness into one continuous real browser E2E and adding per-job remote egress approval when project policy requires it, then final soak/live acceptance once the runnable-stack evidence is ready.

The redaction slice is now active and partially validated. The new matrix covers all currently identified secret-bearing output paths and the config package gate passes; final closure still needs the broader redaction-adjacent package validation and full Increment 2 acceptance.

DOD-11 intelligence acceptance evidence is now explicit but remains part of the broader open Milestone 7 final acceptance. The checked matrix covers incremental indexing, context manifests, workflow baselines, differential verification, impact records, cache integrity/quota/retention, and REST/CLI/browser parity without claiming full Increment 2 closure.

DOD-13 verification-safety evidence is now explicit but still part of the broader open Milestone 7 final acceptance. The checked matrix covers newly introduced finding non-waiver, immutable full-suite final verification policy, explicit golden-update approval, and REST/CLI/browser parity.

DOD-14 documentation-policy evidence is now explicit but remains in progress. The checked matrix covers source-controlled generation, render/check validation evidence, blocked documentation findings, fresh verification and context reuse, and REST/CLI/browser parity while preserving the remaining renderer/tool-profile and independent documentation-QC work.

The security/adversarial acceptance boundary is now explicit but remains in progress. The checked matrix binds authorization, stale edits, injection, worker isolation, SSRF/egress/fallback/cost, webhook authentication, cross-project isolation, restore path safety, no-write preview, and redaction evidence to existing tests and docs while final acceptance remains open.

I2-31.2 unit/property/fuzz evidence is implemented. The checked matrix covers current unit/property categories and fuzz seed corpora for imports, manifests, webhooks, archive paths, untrusted Markdown task packets, fake-model packet extraction, and streamed provider event parsing.

I2-31.3 fixture/integration failure evidence is implemented. The checked matrix covers PHP/.NET/R/malformed fixtures, fake forge/provider/local-Git evidence, baseline/candidate and partial-index behavior, stale config, restart/resume, pack and policy lifecycle, cache/memory staleness, documentation render blockers, provider denied manifests, timeout and rate-limit retry circuits, streamed interruption parsing, batch partial failure, egress redaction, and telemetry redaction.

I2-31.4 local-fake E2E evidence is now explicit but remains in progress. The checked matrix maps all 14 required workflow steps to existing deterministic coordinator, UI, API, CLI, provider, configuration, backup, and documentation evidence. The acceptance package drives one controller-level browser-originated local-fake path through onboarding, pack acceptance, configuration rollback, provider route approval simulation, fails-closed remote inference, backup/staged restore/reopen, resumed local publication, and evidence verification. Step 13 is covered by retained-probe model-drift denial and lower-trust fallback denial, worker provider-route snapshots are retained for implementation, repair, Test Designer, Documentation Agent, and QC calls, and the browser now exposes typed provider/endpoint/model/route controls with server-enforced remote route approval. A continuous real browser E2E plus per-job remote approval remain open.

I2-31.5 performance/soak evidence is now explicit but remains in progress. The checked matrix covers scheduler resource safety and fairness, runtime benchmark quality gates, historical evaluation metrics, observability timing/resource diagnostics, deterministic local acceptance smoke evidence, and a bounded retained soak report. The report is deliberately short and deterministic; final closure still needs retained full-run evidence after the remaining local-fake E2E and runnable-stack rows close, including restart/resume behavior during or after the soak window.

I2-DOD-26 runnable Compose/acceptance evidence is now explicit but remains in progress. The checked matrix covers CI, local acceptance scripting, documented Compose views, mock runtime stack startup/health, and maintenance scripts. A final retained full live acceptance run remains open until the remaining local acceptance rows are ready.

## Context and Orientation

The repository root is `/srv/coder`. `cmd/controller` assembles the trusted control plane; `internal/api` owns the REST/OpenAPI surface; `internal/storage` defines ports and `internal/storage/sqlite` implements them; `internal/workflow` is the durable coordinator; `internal/config` currently owns the Increment 1 system document; `cmd/maintainctl` is the recovery/automation CLI; and `web/src/OperatorConsole.tsx` is the browser console. Migrations are embedded from `internal/storage/sqlite/migrations` and are forward-only. External implementations live behind interfaces such as the runner, model, repository, memory, and Git bridge ports.

A configuration descriptor is immutable trusted metadata for one stable setting key: type/schema, help text, category, allowed scopes, safe default, secret classification, required role, apply semantics, dependencies, prerequisites, validation and dry-run capability, and UI hints. A scoped value is a typed override at one of seven ordered scopes: built-in default, system, capability pack, project, named environment/runner, job template, and one-job override. An effective value is the deterministic winner plus every contributing value and provenance. An immutable job configuration snapshot records all effective values and descriptor versions when the job is accepted, so later configuration edits cannot change a running or historical job.

A capability pack is a versioned, checksummed declarative bundle of configuration fragments, toolchain identities, verifier classes, service needs, documentation rules, risk hints, and compatibility metadata. It never contains arbitrary shell, images, mounts, networks, capabilities, paths, or environment fields. Repo Doctor parses untrusted repository evidence in bounded sandboxes and produces confidence-rated proposals; it never applies a pack or configuration automatically.

The code-intelligence index is derived, project-separated data with provenance and partial-failure records. The Context Compiler selects bounded, deterministic stage packets from approved sources and records why each range was selected, its trust and cost, and the reserved output budget. Baselines classify verification observations as pre-existing, resolved, new, changed, or indeterminate; a targeted inner loop may accelerate work but a fresh full required suite still gates publication.

The provider gateway is the only inference-egress path. Provider, endpoint, model, and route profiles are versioned records with write-only credentials, probed capabilities, trust/data-classification policy, budgets, costs, circuit state, and approved endpoint/network behavior. Model names never imply capabilities. Remote inference starts disabled and every outbound packet is scanned, classified, bounded, manifested, and policy-approved.

## Plan of Work

Milestone 1 begins with a `config.Registry` and exhaustive compiled descriptor inventory for every currently configurable Increment 1 field plus the Increment 2 settings introduced as later slices land. Add pure functions for descriptor validation, canonical value validation, seven-scope precedence, provenance, redaction, export/import, dependency checks, and immutable snapshot hashing. Migration 16 adds descriptor-version records, scoped values with optimistic versions, drafts and draft entries, prerequisite/dry-run results, revisions linked to exact scopes, and job snapshots. The service layer performs validation, review, apply, rollback, and audit transactionally. Existing `/config` operations remain compatible while new `/config/descriptors`, `/values`, `/effective`, `/drafts`, `/validate`, `/dry-runs`, `/prerequisites`, `/revisions`, `/export`, and `/import` routes receive typed OpenAPI schemas and ETags. `maintainctl config` gains the same operations. The browser gets a dedicated Configuration page with registry search, basic/advanced views, scope selection, inheritance and provenance, effective preview, diff, validation, prerequisites, dry run, drafts, review/apply/discard, revisions/rollback, redacted export/import/reset, apply-state badges, keyboard operation, and accessible errors. A machine-readable inventory test fails if any descriptor lacks service/API/CLI/UI/test evidence or if a runtime setting has no descriptor.

Milestone 2 adds migration-backed project-local code-intelligence state: repository revisions, blob identities, parse facts, symbols, references, test relationships, dependency relations, index runs, failures, and coverage. Parser inputs are bounded and hostile; language adapters are replaceable and failures remain visible without corrupting prior good facts. The service supports incremental refresh, query, status, coverage, rebuild, and cancellation. The Context Compiler consumes only authorized indexed facts, project memory, task/risk/policy records, and bounded source ranges; it deduplicates and reserves output capacity, persists only a redacted manifest, and supports manifest inspection and comparison. Baseline records and observations bind to repo/config/tool/pack identities and enforce the five-way classification. Test impact records selected tests, reasons, confidence, and omissions while the final-gate policy always schedules a fresh complete required suite. Cache namespaces and keys include project, repository/blob/config/tool/pack/platform identities; integrity, quotas, retention, and audited purge are enforced.

Milestone 3 introduces Repo Doctor scan/proposal/evidence tables and a stepwise browser onboarding flow. Bounded detectors inspect manifests, locks, CI, languages, tool configs, docs, services, and line-ending/platform hints, display confidence/evidence/diffs, and require explicit acceptance. Capability packs use a schema and checksum verifier plus a catalog/assignment lifecycle with trust, compatibility, version comparison, upgrade dry run, apply, rollback, and effective-configuration preview. Ship declarative `php83-intranet`, `r-statistical-validation`, and `sbom-fmea-security` packs with pinned tool identities and fixture-backed checks. No pack may expand runner authority; unavailable tools/services produce prerequisites or skipped-with-policy results, never silent substitution.

Milestone 4 generalizes repository operations into normalized forge objects with explicit provider metadata, cursor/pagination/rate/retry/idempotency/webhook/credential-rotation behavior. Preserve the proven GitHub path, add protocol-accurate fake GitLab and local bare-Git adapters, and expose forge profiles, credentials, mappings, probes, webhook health, cursors, and diagnostics in the browser. Add `windows-dotnet-labautomation` as a declarative pack and a narrow authenticated simulated-Windows worker contract with fixed operation classes, artifact schemas, time/resource limits, and no arbitrary PowerShell. The real Windows adapter remains operator-only. Heterogeneous PHP, R, .NET, mixed, GitHub, GitLab, and local-Git fixtures prove normalization and restart/idempotency behavior.

Milestone 5 adds a versioned task-contract schema and Clarifier stage before implementation. Questions, assumptions, answers, acceptance requirements, edit boundaries, constraints, and approval history are durable and operator-editable. Deterministic risk rules calculate a level and routing requirements from repository/task/pack/policy evidence; explanations are stored, increases are automatic, and decreases require an authorized, reasoned, expiring waiver. Versioned agent input/output contracts require evidence-linked claims and reject hidden reasoning fields. Medium/high-risk work invokes an independent Test Designer whose plan is implemented and verified separately. Golden/rehearsal fixtures gate changes to prompts, packs, policies, routes, parsers, and agent contracts. Documentation has its own policy, agent, render/link/source-map/quality tools, findings, previews, and QC stage. OPA bundles are pinned and versioned; structured templates are the normal editor, advanced Rego is authorized separately, simulation/tests precede activation, rollback is append-only, and errors fail closed.

Milestone 6 adds persistent resource inventory, leases, queue decisions, fairness, deadline, model-residency, provider-limit, and co-residence constraints. Every scheduling decision is explainable and unsafe combinations are rejected. The provider gateway implements local llama plus fake-contract-tested OpenAI Responses, OpenAI Chat, generic compatible, Azure, Anthropic, Gemini/Vertex, and Bedrock families behind one neutral call contract. Endpoint admission enforces HTTPS where applicable, scheme/host/port/path allow-lists, DNS/IP revalidation, private/link-local/metadata denial, redirect policy, size/time limits, write-only secrets, data classifications, egress scanning/manifests, trust tiers, budgets, costs, circuit breakers, batch isolation, resumability, capability probing, and non-escalating fallbacks. The runtime benchmark lab records exact binary/model/hardware/settings identity, compares quality and performance, and keeps experimental profiles off by default and reversible. The evidence graph stores typed provenance edges from request through publication/SBOM. Historical patch evaluation uses isolated ephemeral memory/cache namespaces and reproducible datasets. OpenTelemetry spans/metrics/log correlation are bounded, redacted, local by default, optionally exported only through approved disabled-by-default OTLP profiles; support bundles use the same redaction policy.

Milestone 7 reconciles all browser information architecture into at least the required 17 named areas: Setup and health, Repositories, Capability packs, Jobs, Quality, Documentation, Code intelligence, Forges, Runners and Windows, Models and agents, Scheduling and resources, Policy and risk, Security and SBOM, Evaluation, Memory and evidence, Observability, and Configuration. Each page must perform routine operations rather than display placeholders. Complete migration upgrades from retained databases, backup/restore/rollback of every new domain, restart reconciliation, property/fuzz/security/isolation tests, protocol failure tests, browser E2E and keyboard/accessibility runs, performance thresholds, and bounded soak tests. Finish the operator, provider, Windows, pack-authoring, security, privacy, schema, migration, troubleshooting, and external-validation documents. The final acceptance report links every row in `config/increment-2-coverage.json` to implementation and current test evidence.

## Concrete Steps

Run all commands from `/srv/coder`. This development host requires `sudo` for its rootful Docker daemon; a properly configured rootless host can omit it. Use the supported containerized path and install frontend dependencies inside every ephemeral web-tool invocation:

    sudo docker compose run --rm go-tool gofmt -w cmd internal
    sudo docker compose run --rm go-tool go test ./...
    sudo docker compose run --rm go-tool go test -race ./...
    sudo docker compose run --rm go-tool go vet ./...
    sudo docker compose run --rm web-tool sh -c 'npm ci && npm test'
    sudo docker compose run --rm web-tool sh -c 'npm ci && npm run build'
    sudo docker compose run --rm web-tool sh -c 'npm ci && npx redocly lint ../internal/api/openapi.yaml'
    sudo docker compose run --rm web-tool sh -c 'npm ci && npm run check:api'
    sudo docker compose config --quiet
    ./scripts/acceptance.sh
    sudo docker compose build controller maintainctl runnerd

For each slice, first add failing pure/domain and migration tests, then implement storage and service behavior, then API/OpenAPI and CLI, then the browser workflow and accessibility regression. Run the narrow package/test after each edit, regenerate API types only with the documented npm command, and commit the slice after its coverage rows and this plan are updated.

## Validation and Acceptance

Milestone 1 passes when every Increment 1 setting is described, all seven scopes resolve deterministically with provenance, secrets redact everywhere, concurrent edits conflict through ETags/versions, drafts and reauthentication gates work, rollback is additive, job snapshots cannot change, and UI/API/CLI yield byte-equivalent canonical effective configuration. Import and export are bounded and schema/version checked. Browser tests exercise typed fields, dependency errors, focus/error association, keyboard apply/rollback, and safe/basic versus advanced views.

Milestones 2–4 pass when hostile parser and archive fixtures cannot escape limits or namespaces; incremental facts, contexts, baselines, impacts, and caches bind to exact identities; a new failure cannot be classified away; full final verification cannot be skipped; Repo Doctor never auto-applies; packs are checksummed, pinned, reversible, and authority-neutral; GitHub behavior is unchanged; GitLab/local Git normalize and recover correctly; and the simulated Windows worker rejects unregistered operations and returns deterministic artifacts.

Milestones 5–6 pass when task approval precedes implementation, medium/high risk deterministically invokes the required independent stages, risk lowering and policy activation are permissioned/audited/expiring/reversible, agent and documentation claims point to stored evidence, scheduler decisions prevent unsafe co-residence, every provider family passes the same fake conformance suite, forbidden egress fails closed before DNS/connect, fallbacks never raise data exposure or authority, costs/budgets/circuits survive restart, optimizations cannot activate without quality evidence, evaluation cannot see project memory/cache, evidence paths are complete, and telemetry/support bundles contain no seeded secrets.

Final acceptance reruns all Increment 1 checks plus every Increment 2 unit, property, fuzz, integration, migration, browser, E2E, performance, soak, security, backup/restore, restart, and Compose/image gate. The 14-step browser-to-publication scenario in the Increment 2 contract must run with local fakes. External checks are not claimed unless actual authorized infrastructure is available; their fake evidence and exact finite operator procedure must still be present.

## Idempotence and Recovery

Every migration is forward-only and transactional. New state-changing services accept expected versions and idempotency keys where retries can occur. Registry apply, pack assignment, policy activation, baseline acceptance, task approval, provider/profile changes, cache purge, evaluation launch, scheduling leases, and external publication append audit in the same transaction as durable state. Rollback creates a new revision or assignment rather than mutating history. Derived indexes and caches are rebuildable from exact identities; partial runs never replace the last complete generation. Secrets are never recoverable through APIs or exports and restoration requires operator-supplied secret material.

Jobs retain their immutable configuration, task, policy, pack, route, provider-capability, baseline, context, and scheduler snapshots. On restart the controller reconciles leases and uncertain external effects from durable idempotency records instead of repeating them blindly. Provider batches, forge webhooks/polling, indexing, evaluation, and telemetry export use cursors and bounded retries. Backup manifests version every new table/artifact class and restore defaults to dry-run with checksum/schema/compatibility validation.

## Artifacts and Notes

Pre-change baseline at commit `f318466`:

    $ sudo docker compose run --rm go-tool go test ./...
    all packages passed

    $ sudo docker compose run --rm web-tool sh -c 'npm ci && npm test'
    1 test file, 2 tests passed; no critical accessibility violations

    $ sudo docker compose config --quiet
    exited 0

    $ sudo docker compose run --rm web-tool sh -c 'npm ci && npm run check:api'
    generated OpenAPI client matched

The first unauthenticated `docker compose` invocation failed because this host user cannot access `/var/run/docker.sock`; this matches the known Increment 1 development-host limitation and all successful baseline commands used `sudo`. The initial `npm run check:api` without `npm ci` also failed as expected for an ephemeral tool container and was rerun through the documented locked-install path.

## Interfaces and Dependencies

New domain packages should expose narrow, implementation-neutral interfaces. Expected packages include `internal/configregistry`, `internal/intelligence`, `internal/contextcompiler`, `internal/baseline`, `internal/testimpact`, `internal/cache`, `internal/repodoctor`, `internal/packs`, `internal/forges`, `internal/taskcontract`, `internal/risk`, `internal/policy`, `internal/testdesigner`, `internal/documentation`, `internal/scheduler`, `internal/providers`, `internal/evidence`, `internal/evaluation`, and `internal/observability`. Names may be consolidated when adjacent concepts share a cohesive boundary, but domain packages must not import browser, Docker, forge, model-provider, Hermes, Windows, or parser implementations.

Prefer the standard library and existing SQLite dependency. Add maintained pinned libraries only where they materially supply a protocol/parser/OPA/OpenTelemetry implementation that cannot be safely reproduced, record licenses and source identities, and update `go.sum` or `web/package-lock.json`. No operational `latest` tag is allowed. Generated OpenAPI clients and built web assets are never hand-edited.

Revision note (2026-07-20 20:46Z): created the Increment 2 plan after a full contract and architecture audit. Recorded the missing base-goal alias, Increment 1 configuration/UI gaps, additive registry decision, seven-milestone dependency order, external-validation boundary, and clean pre-change Go/frontend/Compose/OpenAPI baseline.

Revision note (2026-07-20 21:05Z): completed the pure registry/resolver foundation of Milestone 1. The registry now inventories every Increment 1 `System` field, distinguishes immutable bootstrap values, validates trusted metadata and values, resolves all seven scopes with provenance independent of input order, redacts secrets, rejects unknown keys, and hashes complete snapshots. Persistence, application services, API/CLI, and the workbench remain open.

Revision note (2026-07-20 21:09Z): added migration 16 and the scoped persistence boundary. Existing active Increment 1 configuration imports into the new system scope; version heads reject stale writes; values, redacted revision entries, audit, resets, and immutable per-job snapshots are transactional. Application-level registry validation, drafts/checks, compatibility projection, API/CLI, and UI remain open.

Revision note (2026-07-20 21:27Z): completed the registry application-service slice. Draft edits, validation/dry-run evidence, review, apply, discard-ready transitions, historical rollback, effective resolution, and job snapshot creation now share trusted controller logic; reviewed bytes are bound at the storage transaction and system-scope applies update the Increment 1 compatibility document atomically. API/OpenAPI, CLI, export/import, purpose-built external checks, and UI remain open.

Revision note (2026-07-20 21:38Z): exposed the service through ETag-bound REST/OpenAPI and matching `maintainctl config` commands, initialized the system scope at controller startup, generated typed frontend bindings, and added API/CLI contract regressions. Redocly and generated-client drift pass. Redacted export/import, dependency/prerequisite views, and the browser workbench remain open before Milestone 1 can close.

Revision note (2026-07-20 22:00Z): completed the redacted export/import backend and parity surfaces. Deterministic hashes detect tampering, strict mode rejects unknown keys, forward-compatible mode preserves them only as immutable draft evidence, and reviewed import applies registered values only. Kept committed migration 16 unchanged and introduced migration 17 with a retained-data regression. Also pinned the patched `js-yaml` 4.3.0 compatibility release after advisory-database drift made the unchanged lock fail; the full current Go/frontend/OpenAPI/Compose gate is clean. The browser workbench, setting-level inventory enforcement, dependency/prerequisite handlers, and automatic job-acceptance snapshots remain open.

Revision note (2026-07-20 22:25Z): completed the schema-driven Configuration workbench and setting-level coverage gate, including typed controls, provenance, drafts, checks, import/export, and rollback. Bound complete redacted configuration snapshots atomically to all current job-acceptance paths and exposed them in Job inspection. The refactoring-ui pass scores the workbench 8.5/10 pending a real-browser overflow/focus pass; the host has no `npx`, so the Playwright CLI skill is paused without changing the host. Generic dependency evaluation and real prerequisite handlers remain open before Milestone 1 closes.

Revision note (2026-07-20 22:42Z): closed Increment 2 Milestone 1 after adding typed conditional relation validation, contextual import enforcement, compiled prerequisite and dry-run probes, browser relation/reset/diff affordances, and correct system inheritance in non-system workbench views. Recorded complete Go/vet/frontend/build/OpenAPI/Compose evidence and retained the real-browser overflow/focus run as a precise final-phase check because the skill prerequisite is unavailable on the host.

Revision note (2026-07-20 23:10Z): began Milestone 2 with project-separated incremental analysis, exact bounded Git snapshots, durable context/baseline/differential/impact/cache evidence, typed REST/OpenAPI/CLI operations, and the operational Code intelligence page. Recorded the remaining production workflow integration and parser/cache enrichment explicitly instead of treating the first replaceable adapters as final.

Revision note (2026-07-20 23:45Z): integrated Milestone 2 evidence into the durable workflow and immutable configuration path. Added migration 19 for replay-safe identities, automatic sync indexing, clean pre-change baseline capture, purpose-bound differential production, per-candidate test impact, mandatory final full-suite evidence, real parsed-blob cache integrity/quota/retention/hit evidence, bounded cancellation, and six typed intelligence/verification controls. Kept the milestone open for the richer adapters and remaining operator actions listed in Progress.

Revision note (2026-07-21 00:01Z): added project-scoped context-manifest comparison and cache integrity verification, quota simulation, and trusted-snapshot warming across service/storage, REST/OpenAPI/generated client, CLI, and the Code intelligence workbench. Verification recomputes parsed-object identities and reports unsupported kinds as unavailable. Kept Milestone 2 open for richer adapters, broader context sources, baseline correction/update policy, and impact overrides/history.

Revision note (2026-07-21 00:16Z): added migration 20 and append-only baseline supersession, differential correction, and targeted-test override ledgers with attribution, reason, reauthentication, audit, REST/OpenAPI/generated-client/CLI/browser parity, and invariant regressions. Newly introduced findings cannot be classified away and full-suite policy is not overrideable. Rich adapters and broader context sources remain before Milestone 2 closure.

Revision note (2026-07-21 00:46Z): closed Milestone 2 with a real pinned multi-language Tree-sitter service, bounded SCIP/read-only-LSP enrichment contract, parser/tool provenance, and production context sources spanning immutable configuration, policy, instructions/docs, exact indexed ranges, verified memory, findings, and verification/impact evidence. Preserved the static non-CGO controller, proved both images build, and passed live internal service probes.

Revision note (2026-07-21 01:31Z): closed Milestone 3 with migration-backed checksummed capability lifecycle, independent exact project assignments, no-write Repo Doctor scans, optimistic evidence reviews, PHP/R/security catalogs, golden/rehearsal primitives, and matching API/CLI/browser/operator documentation. Recorded the version-separation and trusted-snapshot decisions and advanced the active work boundary to forge normalization and the simulated Windows worker.

Revision note (2026-07-21 02:38Z): recorded the partial Milestone 4 forge foundation before pausing Increment 2 for an operator-directed product rename and first GitHub synchronization. Migration 22, the credential-isolated normalized provider boundary, GitLab configuration and draft-MR support, durable profiles/objects/cursors/idempotency, endpoint and reauthentication policy, REST/OpenAPI/client generation, CLI operations, and focused regressions are present; hosted inventory, browser workbench, Windows simulation, heterogeneous fixtures, and the milestone acceptance gate remain explicitly open.

Revision note (2026-07-21 02:49Z): renamed the product to CodeMaintainer and the Go module to `github.com/B1-Mordred/CodeMaintainer` for initial synchronization with the empty canonical GitHub repository. Updated display branding, imports, package/Compose/service identifiers, schema namespaces, examples, generated metadata, and operator evidence while deliberately retaining stable environment variables, executable names, contract filenames, database schema, and legacy backup encryption context.

Revision note (2026-07-22 08:13Z): resumed Milestone 4 with bounded paginated GitHub/GitLab hosted inventory, transient-read retry and rate-limit continuation, explicit unsupported-capability evidence, exact registered-project identity enforcement, read-only GitHub Actions permission, and a responsive provider-neutral forge workbench. Kept the milestone open for GitLab webhook normalization, restart/API failure coverage, Windows simulation, fixtures, documentation, and the complete gate.

Revision note (2026-07-22 08:40Z): paused at the operator-requested Milestone 4 checkpoint after adding migration 23, the closed Windows-worker domain/protocol, deterministic simulator, .NET lab automation pack, durable replay-safe evidence, controller/API/OpenAPI/CLI integration, and the accessible Runners and Windows workbench. Full Go tests/vet, frontend accessibility tests/build, generated API drift, zero-warning OpenAPI lint, and Compose validation pass; remote-adapter routing, heterogeneous fixtures, remaining forge/restart failures, documentation, and Milestone 4 closure remain explicit next work.

Revision note (2026-07-22 09:07Z): paused and synchronized at a second Milestone 4 checkpoint after adding fail-closed remote Windows-worker routing with fixed-directory secret resolution, PHP/.NET/R/malformed heterogeneous fixtures, GitLab merge-request webhook normalization and forward-only migration 24, operator documents, generated-client parity, and coverage-map evidence. Full Go tests/vet, focused race, frontend accessibility tests/build, Redocly/client drift, and Compose validation pass; the specialized capability-editor audit, broader restart/migration/failure matrix, and complete Milestone 4 gate remain open.

Revision note (2026-07-22 10:02Z): closed Milestone 4 after making trusted manifest schemas authoritative for typed R/security proposal and assignment configuration, adding optimistic audited API/CLI/browser updates and restart-safe normalization, completing the forge/Windows/capability migration-restart-failure matrix, and publishing specialized pack documents and coverage evidence. Full Go/vet/focused-race, ten frontend accessibility tests/build, OpenAPI/client drift, Compose, image, authenticated health/doctor, and isolated desktop/mobile Chromium gates pass. The first acceptance attempt exposed retained-port/session assumptions; the successful isolated rerun preserved and rechecked the existing pre-rename appliance.

Revision note (2026-07-22 10:11Z): recorded the operator-requested pause after the Milestone 4 closure commit reached `origin/dev` and GitHub Actions run `29910740641` completed successfully. Milestone 5 remains the next unstarted scope, and the untracked Increment 3 goal document remains untouched.

Revision note (2026-07-23 21:40Z): started Increment 2 Milestone 5 with controller-owned task contracts and deterministic risk routing. Added migration 25, the `awaiting_task_approval` gate, exact-version contract approval, risk assessment/waiver storage, API/OpenAPI/generated-client/CLI/browser visibility, and `docs/quality-workflow.md`. Validation passed for `go test ./...` and frontend check:api/tests/typecheck/Redocly. Milestone 5 remains open for schema-constrained agent contracts, Test Designer, golden/rehearsal approval, documentation policy/QC, and OPA.

Revision note (2026-07-23 21:45Z): paused at the operator-requested Milestone 5 foundation checkpoint for git synchronization. The checkpoint is intentionally partial: task approval and deterministic risk routing are durable and visible, while independent agent routing, golden/rehearsal policy, documentation-claim QC, and OPA enforcement remain the next open work.

Revision note (2026-07-23 22:25Z): advanced Milestone 5 with the versioned agent-contract registry and validation evidence ledger. Added migration 26, strict JSON Schema response-format requests for implementation/QC, schema/payload hash validation records, API/OpenAPI/generated-client/CLI/browser visibility, and quality-workflow documentation. The checkpoint remains partial pending bounded validation retry orchestration, independent Test Designer execution, golden/rehearsal approval, Documentation Agent/QC, and OPA policy lifecycle.

Revision note (2026-07-23 23:10Z): paused and synchronized Milestone 5 at the independent Test Designer checkpoint. Added migration 27, `test_designer` worker/model routing, exact contract/risk/result-bound task packets, strict `test_proposal` decoding, append-only report storage, workflow routing for medium/high risk, low-risk skipped evidence, REST/OpenAPI/generated-client/CLI/browser visibility, report context reuse, and updated coverage/docs. Validation passed for `go test ./...`, `go test -race ./...`, `go vet ./...`, frontend check:api/tests/typecheck/build/Redocly, and Compose validation.

Revision note (2026-07-23 23:35Z): resumed Milestone 5 and added the golden/rehearsal gate foundation. Added migration 28, `internal/golden`, append-only report and approval storage, the `golden_rehearsal_review` workflow state, registered capability/Test Designer rehearsal selection, fail-closed approval-required changed-golden behavior, API/OpenAPI/generated-client/CLI/browser evidence surfaces, QC context reuse, and updated quality workflow docs plus coverage. Validation passed for focused Go packages, `go test ./...`, frontend check:api/tests/typecheck, and Redocly lint.

Revision note (2026-07-23 23:39Z): advanced Milestone 5 with the Documentation Agent foundation. Added migration 29, `internal/docagent`, append-only documentation manifest storage, `documentation` worker/model routing, `loading_documentation_model` and `documentation_review` workflow states, exact contract/risk/result-bound manifest validation, source-controlled documentation commit handling with fresh verification, API/OpenAPI/generated-client/CLI/browser evidence surfaces, and updated quality workflow docs plus coverage. Validation passed for focused Go packages and frontend check:api/tests/typecheck/Redocly.

Revision note (2026-07-24 00:18Z): advanced Milestone 5 with the OPA policy lifecycle foundation. Added migration 30, `internal/policy`, append-only bundle/activation/simulation/decision storage, the `policy_review` workflow state, active bundle decision recording before QC, redacted policy inputs, API/OpenAPI/generated-client/CLI/browser evidence surfaces, and updated quality workflow docs plus coverage. Validation passed for focused Go packages, `go test ./...`, `go vet ./...`, frontend check:api/tests/build, OpenAPI lint, and Compose validation. Full Rego authoring, policy tests/coverage UI, rollback workbench, and cross-domain protected-action enforcement remain open.

Revision note (2026-07-24 00:48Z): paused at the operator-requested git sync point after adding embedded OPA/Rego advanced bundle authoring and retained policy test evidence. Added migration 31, OPA v1 module dependencies, formatted-source hashing, advanced activation gating, API/OpenAPI/generated-client/CLI operations for create/test/test-runs, and a Policy page with simulation, test coverage, activation history, source inspection, and expert Rego authoring. Validation passed for focused Go packages, `go test ./...`, `go test -race ./...`, `go vet ./...`, frontend check:api/tests/build, OpenAPI lint, and Compose validation. Increment 2 remains active and incomplete.

Revision note (2026-07-24 01:09Z): resumed Milestone 5 and added Test Designer disposition enforcement. Added migration 32, `awaiting_test_design_disposition`, append-only proposal disposition storage, effective disposition overlays, API/OpenAPI/generated-client/CLI/browser actions, and workflow regression coverage that pauses before later quality gates until proposals are explicitly resolved. Focused Go and frontend checks pass; the full Increment 2 objective remains active.

Revision note (2026-07-24 01:28Z): completed the bounded structured-output retry slice for agent contracts. The model worker now retries implementation, QC, Test Designer, and Documentation Agent outputs according to the registered descriptor policy, passes controller validation errors back as bounded feedback, and fails closed after three invalid attempts without applying edits or accepting malformed evidence. Focused tests passed for `internal/agents`, `internal/workflow`, and `cmd/maintainer-worker`.

Revision note (2026-07-24 01:37Z): advanced the golden/rehearsal gate from terminal approval-required failure to an auditable operator wait. Added `awaiting_golden_approval`, latest append-only approval resolution, API transition back to documentation loading after all required comparisons are approved, OpenAPI/generated-client updates, and Jobs page approval/rejection controls. Focused Go packages, frontend tests/build, generated-client drift, and JSON validation pass.

Revision note (2026-07-24 01:53Z): added typed golden comparison tolerance/mask profiles as append-only review evidence. Added migration 33, profile domain validation, SQLite persistence, REST/OpenAPI/generated-client support, `maintainctl golden configure-profile`, Jobs page controls, structured diff preview evidence, and updated coverage/docs. Focused Go packages and frontend API drift/tests/build pass; Increment 2 remains active and incomplete.

Revision note (2026-07-24 02:09Z): added documentation-policy finding lifecycle integration. Blocked Documentation Agent manifests now create stable retained findings for pending/blocked requirements, failed checks, unsupported claims, or generic blocked policy evidence; repair-cycle review allows only documentation policy categories as new blockers. The Jobs page exposes documentation finding counts/evidence, and coverage/docs are updated. Focused workflow/storage tests and frontend API drift/tests/build pass; Increment 2 remains active and incomplete.

Revision note (2026-07-24 02:30Z): added the first documentation policy simulation surface. The controller now exposes a versioned built-in documentation policy profile and deterministic simulation result for path/class/risk/language/pack/label evidence, with REST/OpenAPI/generated-client, CLI, and Policy page support. Focused Go and frontend checks pass; persistent editable documentation policy/tool profiles, render previews, and independent documentation QC remain open.

Revision note (2026-07-24 03:25Z): paused at the operator-requested sync point after adding the advisory resource scheduler foundation. Added migration 34, `internal/scheduler`, retained scheduler decisions, REST/OpenAPI/generated-client/CLI status/simulation/history operations, Automation page topology/profile/simulation UI, `docs/scheduler.md`, and coverage-map evidence. Focused Go and frontend checks pass; live scheduler lease enforcement and the rest of Milestone 6 remain open.

Revision note (2026-07-24 03:55Z): bound live job lease acquisition to scheduler decisions. Active leases and resumable candidates are converted to bounded scheduler queue items inside the acquisition transaction; the selected decision and lease commit together, while fairness and CPU/RAM deferrals persist without granting a lease. Added state-to-profile mapping and regressions for same-project and memory-pressure deferrals. Focused scheduler/storage/queue tests, full Go tests, and Go vet pass; broader Milestone 6 provider/runtime/evidence/observability work remains open.

Revision note (2026-07-24 04:20Z): added the provider-gateway foundation. Migration 35 persists provider, endpoint, model, route, and append-only egress manifest records; defaults keep remote providers disabled while allowing local llama.cpp routing; endpoint validation rejects unsafe public/private targets; route simulation enforces data-class, structured-output, token, cost, and enabled-profile constraints and retains allow/deny manifests. REST/OpenAPI/generated-client, CLI, Models-page UI, docs, and coverage evidence are updated. Focused provider/storage/API/CLI tests and targeted frontend API/test/build gates pass; Increment 2 remains active and incomplete.

Revision note (2026-07-24 04:25Z): paused at the requested git-sync checkpoint after adding provider capability probes. Migration 36 stores immutable probe observations; fake adapters cover local llama.cpp, OpenAI Responses, OpenAI Chat Completions, OpenAI-compatible, Azure OpenAI, Anthropic Messages, Gemini, Vertex Gemini, Bedrock Converse, and Bedrock Responses-compatible families without external calls; API/OpenAPI/generated-client, CLI, Models-page probe controls, docs, and coverage evidence are current. Increment 2 remains active and incomplete.

Revision note (2026-07-24 04:42Z): advanced Milestone 6 with a retained runtime benchmark lab. Migration 37 stores append-only benchmark runs; `/models/{profileID}/actions/benchmark` now records exact runtime identity, quality/determinism gates, prompt/decode timing, duration, memory, cache eligibility, experimental feature gates, and recommendation evidence; `maintainctl model benchmarks`, OpenAPI/generated-client, Models-page runtime lab, docs, and coverage map are current. Increment 2 remains active and incomplete.

Revision note (2026-07-24 05:08Z): advanced Milestone 6 with the first typed evidence graph checkpoint. Added migration 38, `internal/evidence`, append-only node and edge storage, transaction-bound artifact-to-job graph indexing, `GET /jobs/{jobID}/evidence-graph`, `maintainctl evidence <job-id>`, Jobs-page traceability display, docs, OpenAPI/generated-client updates, and coverage evidence. Focused Go and targeted web/OpenAPI checks pass; full request-through-publication/SBOM trace coverage, historical evaluation, observability, and support bundles remain open.

Revision note (2026-07-24 05:35Z): added the historical evaluation foundation and prepared the requested sync checkpoint. Migration 39 stores append-only datasets and runs; `internal/evaluation` creates reproducible offline comparison reports, hides known patch identity, requires isolated `eval://memory/...` and `eval://cache/...` namespaces, records review-only profile metrics, and rejects project-memory leakage. REST/OpenAPI/generated-client, `maintainctl evaluation`, Evaluation-page UI, docs, and coverage map are current. Increment 2 remains active and incomplete.

Revision note (2026-07-24 06:20Z): paused at the requested sync checkpoint after adding the first observability/support-bundle foundation. Migration 40 retains append-only redacted telemetry events and bounded support bundle manifests; sensitive keys, prompts, request bodies, unrestricted source content, and hidden reasoning are redacted before storage/export metadata. REST/OpenAPI/generated-client, `maintainctl observability`, Observability-page UI, docs, and coverage evidence are current. External OTLP remains disabled/default-off and broader component instrumentation remains open.

Revision note (2026-07-24 06:45Z): started Milestone 7 with the 17-area dashboard information architecture checkpoint. `web/src/OperatorConsole.tsx` now uses the contract labels, dedicated Documentation and Security/SBOM pages expose existing controller-backed policy, manifest, pack, finding, rehearsal, and release-evidence data, and `docs/dashboard.md` records the page map plus remaining validation boundary. `go test ./internal/config` and the full frontend test/build/API-drift gate pass; final dashboard/config deep-linking and full E2E remain open.

Revision note (2026-07-24 06:55Z): added the dashboard/configuration navigation contract for Milestone 7. Non-configuration pages expose filtered Configuration links, Configuration route parsing preserves source-page context, registry search handles multi-token filters, and setting cards render consumer backlinks. `docs/dashboard.md` and the coverage map record the updated boundary; focused App and Configuration tests pass, with full browser/keyboard E2E still open.

Revision note (2026-07-24 07:20Z): advanced Milestone 7 migration/backup evidence with `CurrentSchemaVersion`, centralized embedded migration enumeration, `config/migration-coverage.json`, `docs/migrations.md`, and a regression that cross-checks schema version, migration files, embedded migrations, durable records, restart/resume evidence, backup/restore evidence, documentation, and rollback guidance. Restore tests now verify pre-restore database preservation and stale WAL/SHM cleanup. Focused validation is pending; broader retained-version and full acceptance gates remain open.

Revision note (2026-07-24 07:35Z): added checked external-validation coverage for the eight operator-only acceptance areas. The inventory names prerequisites, safe fake evidence, operator procedures, and CI non-claims for rootless worker topology, licensed model weights, GitHub App installation, OpenViking embeddings, Hermes, remote auth/TLS, separate-host encrypted restore, and OCI scanning. Focused validation is pending; final acceptance reporting remains open.

Revision note (2026-07-24 07:55Z): added checked configuration operation parity coverage. `config/configuration-parity.json` maps typed registry and legacy compatibility operations to OpenAPI routes, `maintainctl config` commands, browser surfaces, docs, and tests; `internal/config/configuration_parity_test.go` verifies required operations, OpenAPI path presence, CLI command presence, and evidence files. Focused validation is pending; full E2E and final acceptance remain open.

Revision note (2026-07-24 08:05Z): paused at the operator-requested sync point without starting the next redaction slice. `f6d25b6` was already pushed to `origin/dev` and CI run `30076306125` passed; this note records `I2-DOD-07` as the next resumption boundary and keeps Increment 2 active and incomplete.

Revision note (2026-07-24 08:25Z): added the checked write-only secret redaction inventory for `I2-DOD-07`. `config/redaction-coverage.json` covers configuration secrets, import/export, provider egress manifests, observability events, support bundles, project memory, auth/session credentials, forge credentials, Windows-worker credentials, and policy decision inputs; the new config test verifies required coverage and proof snippets. Increment 2 remains active pending broader validation and final acceptance.

Revision note (2026-07-24 08:45Z): added the checked DOD-11 intelligence evidence inventory. `config/intelligence-coverage.json` maps incremental indexing, deterministic context manifests, baseline capture, differential verification, test-impact records, cache integrity/quota/retention, and operator parity to existing implementation, UI, docs, and proof snippets; Increment 2 remains active pending final acceptance.

Revision note (2026-07-24 09:05Z): added the checked DOD-13 verification-safety inventory. `config/verification-safety-coverage.json` maps new-failure non-waiver, final full-suite non-waiver, golden-update approval, and operator parity to existing implementation, UI, docs, and proof snippets; Increment 2 remains active pending final acceptance.

Revision note (2026-07-24 09:25Z): added the checked DOD-14 documentation-policy inventory. `config/documentation-policy-coverage.json` maps policy impact selection, source-controlled documentation generation, render/check validation blockers, fresh verification/traceability, and operator parity to existing implementation, UI, docs, and proof snippets while retaining known remaining documentation QC/tool-profile work; Increment 2 remains active.

Revision note (2026-07-24 09:45Z): added the checked security/adversarial inventory for `I2-28` and `I2-DOD-23`. The matrix binds authorization, reauthentication, stale-edit, injection, worker isolation, SSRF/egress/fallback/cost, webhook authentication, cross-project isolation, restore path safety, no-write preview, and redaction boundaries to existing evidence, and backup restore now has an explicit unsafe pending-path regression. Increment 2 remains active pending final acceptance.

Revision note (2026-07-24 10:05Z): added the checked unit/property/fuzz inventory for `I2-31.2` and real Go fuzz entrypoints for declarative imports, model manifests, forge webhooks, backup restore paths, untrusted Markdown task packets, and fake-model packet extraction. At that checkpoint, streamed provider event parser fuzzing was not implemented yet.

Revision note (2026-07-24 10:40Z): added the checked fixture/integration failure inventory for `I2-31.3`, extended the PHP fixture with npm/Vite/Tailwind files, and added a remote-provider denied-manifest regression for cost-circuit and capability-drift cases. At that checkpoint, provider timeout, rate-limit retry, streamed interruption, and batch partial-failure execution fakes were not implemented yet.

Revision note (2026-07-24 11:15Z): added the checked local-fake E2E inventory for `I2-31.4` and strengthened the deterministic coordinator E2E to assert retained evidence graph nodes and edges for the local publication path. The row remains in progress because browser-originated onboarding/pack/config/provider/restore flows are still proven by focused tests rather than one continuous local-fake workflow.

Revision note (2026-07-24 11:45Z): added the checked performance/soak inventory for `I2-31.5` and `docs/performance-soak.md`. The matrix proves current scheduler, runtime benchmark, evaluation, observability, and deterministic smoke evidence while keeping long-duration soak thresholds and retained pass/fail reports as explicit remaining work.

Revision note (2026-07-24 12:05Z): added the checked runnable Compose/acceptance inventory for `I2-DOD-26` and `docs/compose-acceptance.md`. The matrix proves current CI, local acceptance script, documented Compose views, mock runtime stack, and maintenance-script evidence while keeping the final retained full live acceptance run open.

Revision note (2026-07-24 11:20Z): closed `I2-31.2` by adding `internal/providers/stream.go` and `internal/providers/stream_test.go`. The provider gateway now has bounded streamed event parsing and seed fuzz coverage for malformed chunks, interleaved tool-call deltas, cancellation, truncation, and partial usage events. `config/unit-property-fuzz-coverage.json` is all-covered; `I2-31.3` remains open for timeout, rate-limit retry, and batch partial-failure execution fakes.

Revision note (2026-07-24 11:43Z): paused at the operator-requested git sync point. The implementation checkpoint remains `87d2e40` on `origin/dev` with passing GitHub Actions run `30090138123`; this revision records the pause and keeps Increment 2 active and incomplete.

Revision note (2026-07-24 12:00Z): closed `I2-31.3` by adding `internal/providers/execution.go` and `internal/providers/execution_test.go`. The provider gateway now has deterministic no-network execution fakes for timeout retry exhaustion, rate-limit retry-after circuits, streamed truncation, project-isolated batch enforcement, cross-project batch rejection, and per-item batch partial-failure evidence. `config/integration-failure-coverage.json` is all-covered; Increment 2 remains active and incomplete.

Revision note (2026-07-24 12:35Z): added `internal/acceptance/local_fake_e2e_test.go`, a controller-level continuous local-fake acceptance harness that combines fixture onboarding, Repo Doctor acceptance, configuration rollback, provider fake egress simulation, deterministic workflow execution, backup/staged restore/reopen, resumed local bare-Git publication, and restored evidence verification. This creates a tested pause checkpoint but does not close `I2-31.4`; the remaining work is the real browser E2E, worker/provider route snapshot integration, and lower-trust fallback denial.

Revision note (2026-07-24 12:45Z): closed local-fake step 13 by tightening provider route selection. Remote models now require a retained successful capability probe whose observed model ID and capabilities still match the pinned profile, and routes with same-or-higher trust fallback policies establish the first eligible model's trust tier before falling back. The new provider tests deny observed model drift and lower-trust public fallback with retained manifests; `I2-31.4` remains open for the browser E2E and worker provider-route snapshot integration.

Revision note (2026-07-24 13:15Z): paused at the operator-requested sync point without starting worker provider-route snapshot integration. The branch is synchronized to `origin/dev`; the last implementation checkpoint remains `004c1d8` with passing GitHub Actions run `30095028408`. `LOCAL_CODE_MAINTAINER_INCREMENT_3_GOAL.md` is added as the next-increment contract, while Increment 2 remains active and incomplete.

Revision note (2026-07-24 13:45Z): implemented worker provider-route snapshot usage for Increment 2 local-fake evidence. The coordinator now routes implementation, repair, Test Designer, Documentation Agent, and QC worker packets through `internal/providers` before worker execution; phase outcomes retain provider route IDs, provider/model/profile identifiers, and egress manifest hashes without exposing credentials or endpoint URLs to workers. Provider defaults add local worker routes for all model-backed roles while preserving explicit remote documentation simulations, and older databases receive missing built-ins without overwriting existing profiles. Focused providers/workflow/storage/acceptance tests pass; `I2-31.4` remains open for the real browser E2E and editable provider approval controls.

Revision note (2026-07-24 14:10Z): added typed provider gateway profile editing. The provider service now exposes update-only, version-checked provider, endpoint, model, and route mutations for registered IDs; route edits reject missing model references and require explicit egress-preview approval plus rationale before enabling a remote-capable route. REST/OpenAPI/generated TypeScript bindings and the Models-page workbench are updated, and App tests exercise provider and route saves. Focused provider/API Go tests and frontend API drift/tests pass; `I2-31.4` remains open for one continuous browser-to-controller local-fake E2E and policy-driven per-job remote approval.

Revision note (2026-07-24 14:28Z): paused at the operator-requested git sync point without starting the real-browser E2E slice. The implementation checkpoint remains `3082de3` on `origin/dev` with passing GitHub Actions run `30100083452`; the only untracked local file is the Codex resume marker and it is intentionally excluded from repository sync. Increment 2 remains active and incomplete, with `I2-31.4` still the next resumption boundary.
